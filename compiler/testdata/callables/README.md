# compiler/testdata/callables

Source fixtures for callable tests.

Use named subdirectories for behavior families so test code can stay small:

- `captures`
- `mutable_global`
- `cross_module`
- `throwing`

