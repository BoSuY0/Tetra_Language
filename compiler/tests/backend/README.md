# compiler/tests/backend

Backend and object-format integration tests belong here when they can run
through stable compiler/testkit APIs.

Use subdirectories for target families when the tests grow.

