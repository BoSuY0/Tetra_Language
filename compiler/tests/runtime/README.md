# compiler/tests/runtime

Runtime-facing compiler behavior tests belong here when the same scenario
should validate generated artifacts or target runtime contracts.

Use `compiler/testdata` for reusable source fixtures.
