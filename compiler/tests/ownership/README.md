# compiler/tests/ownership

Black-box ownership and move/borrow behavior tests belong here when they can
exercise the compiler through public testkit boundaries.

Package-private ownership internals remain near the implementation until they
can be expressed as domain fixtures.
