# compiler/tests/callables

Function-typed callable behavior tests belong here after the shared build/run
helpers are extracted to `compiler/internal/testkit`.

Planned groups:

- captured closures
- mutable globals
- cross-module callable flow
- throwing callables
- struct and enum payload storage

