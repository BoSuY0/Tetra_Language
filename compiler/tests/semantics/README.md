# compiler/tests/semantics

Semantic checker regression tests belong here when they can run through public
or testkit-backed compiler APIs.

Prefer small files by language feature or diagnostic family.

