# compiler/tests/frontend

Frontend parser, lexer, and diagnostic fixture tests belong here when they are
not tied to package-private internals.

Fixture files should live under `fixtures/` or `compiler/testdata/frontend`.

