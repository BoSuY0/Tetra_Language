# compiler/tests/lowering

Lowering-focused tests belong here when they exercise IR or ABI output through
stable testkit helpers.

Keep backend-specific assertions in `compiler/tests/backend` unless the lowerer
owns the behavior.

