# MEM-ASYNC-010 Workflow Notes

- v9 / `MEM-STORAGE-009` is accepted as `validated_narrow` but remains narrow:
  no broad allocation correctness or "Memory 100%" claim.
- v10 should extend v4 async/task/actor boundary evidence and v9
  storage/lowering conservatism without creating a separate memory truth source.
- Current unrelated dirty worktree entries: `README.md`, `docs/assets/`.
