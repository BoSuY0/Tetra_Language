# P1 Discovery Result

Existing milestone artifacts were already present but not fully strict. Gaps:
validator did not require exact focus order, ResetButton-to-TextBox Tab wrap,
keyboard-routed button actions, Row sibling non-overlap, or Column visual order.
`surface_tree_app.tetra` also routed pointer hit testing through direct
component-rect branches instead of a component tree helper.
