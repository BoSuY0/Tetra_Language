# cli/testkit

Shared CLI test helpers live here once extracted from `cli/cmd/tetra` tests.

Keep command execution, fixture projects, stdout/stderr assertions, and JSON
helpers here. Do not put command-specific expectations in this package.

