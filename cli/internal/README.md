# cli/internal

CLI-only internal packages belong here when `cli/cmd/tetra` needs reusable
implementation that should not become part of the public compiler API.

Shared test helpers belong in `cli/testkit`.
