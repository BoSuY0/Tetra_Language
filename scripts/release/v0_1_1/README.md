# scripts/release/v0_1_1

`gate.sh` is the historical v0.1.1 release gate entrypoint.

Keep v0.1.1-specific release behavior in this directory. Do not add
root-level compatibility wrappers under `scripts/`.
