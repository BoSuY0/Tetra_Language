# scripts/release/v0_1_3

`gate.sh` is the historical v0.1.3 release gate entrypoint.

Keep v0.1.3-specific release behavior in this directory. Do not add
root-level compatibility wrappers under `scripts/`.
