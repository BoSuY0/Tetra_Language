# scripts/release/v0_2_0

`gate.sh` is the historical v0.2.0 release gate entrypoint.

Keep v0.2.0-specific release behavior in this directory. Do not add
root-level compatibility wrappers under `scripts/`.
