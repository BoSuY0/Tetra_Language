# scripts/release/v0_4_0

`gate.sh` is the v0.4.0 release gate entrypoint.

`security-review.sh` owns the v0.4.0 security-review validation boundary.

Keep v0.4.0-specific release behavior in this directory. Do not add
root-level compatibility wrappers under `scripts/`.
