# scripts/release/v0_1_2

`gate.sh` is the historical v0.1.2 release gate entrypoint.

Keep v0.1.2-specific release behavior in this directory. Do not add
root-level compatibility wrappers under `scripts/`.
