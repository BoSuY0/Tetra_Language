# Dogfood Web UI

Small web UI dogfood project for browser smoke. It covers state, binding,
event-to-command wiring, style metadata, and accessibility metadata while
keeping `main` deterministic for the current wasm runner.

Expected browser result marker: `ok:0`.

