# Dogfood CLI

Small command-line dogfood project for the v1 release gate. It exercises module
imports, stable core stdlib calls, test extraction, docs generation, stdout, and
an exit-code contract.

Expected run output:

```text
tetra dogfood cli: ok
```

Expected exit code: `0`.

