// Islands debug smoke test
// Deliberately triggers double-free detection (expected exit code 2 with --islands-debug).
func main() -> i32
uses alloc, capability, islands, mem:
    unsafe:
        var isl: island = core.island_new(64)
        free(isl)
        free(isl)
    return 0
