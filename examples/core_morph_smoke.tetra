// Core lib.core.morph recipe smoke.
module examples.core_morph_smoke

import lib.core.morph as morph

func main() -> Int:
    let recipe: morph.Recipe = morph.recipe_control_action()
    if morph.recipe_expands_to_block(recipe) && morph.capsule_self_check():
        return 0
    return 1
