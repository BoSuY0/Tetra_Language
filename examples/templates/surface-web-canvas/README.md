# surface-web-canvas

Browser-canvas Surface scaffold for the scoped wasm32-web Surface path.

```sh
tetra new surface-app --template surface-web-canvas my-web-surface
tetra surface dev --project my-web-surface --once
```
