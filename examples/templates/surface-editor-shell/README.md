# surface-editor-shell

Editor-shell Surface scaffold for tabbed panes, command bars, and text-heavy
layouts.

```sh
tetra new surface-app --template surface-editor-shell my-editor
tetra surface dev --project my-editor --once
```
