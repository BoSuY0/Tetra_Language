module app.main

import lib.async_generic_memory as async_generic_memory

func main() -> Int
uses alloc, capability, mem:
    let value: Int = async_generic_memory.fallback()
    if value == 42:
        return 0
    return value
