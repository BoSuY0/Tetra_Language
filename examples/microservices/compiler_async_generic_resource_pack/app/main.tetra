module app.main

import lib.async_generics as async_generics

func main() -> Int
uses alloc, capability, mem, runtime:
    let value: Int = async_generics.fallback()
    if value == 42:
        return 0
    return value
