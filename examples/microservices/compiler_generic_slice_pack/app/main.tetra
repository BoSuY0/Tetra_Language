module app.main

import lib.slices as slices

func main() -> Int
uses alloc, mem:
    var raw: []u8 = core.make_u8(4)
    raw[0] = 5
    raw[1] = 10
    raw[2] = 12
    raw[3] = 15
    let total: Int = slices.score(raw)
    if total == 42:
        return 0
    return total
