module app.main

import lib.async_throw_wrappers as async_throw_wrappers

func main() -> Int
uses alloc, capability, mem, runtime:
    let value: Int = async_throw_wrappers.fallback()
    if value == 42:
        return 0
    return value
