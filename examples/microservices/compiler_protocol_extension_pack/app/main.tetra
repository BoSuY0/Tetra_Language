module app.main

import app.ext as ext
import engine.core as core

func main() -> Int:
    let value: core.Vec2 = core.Vec2(x: 19, y: 23)
    let total: Int = core.Vec2.score(value)
    if total == 42:
        return 0
    return total
