# v0.3.0 Static Release Gate Tests

This package owns the static, report-directory, residual-risk, and final-summary
v0.3.0 release gate tests that used to live in the root `tools/scriptstest`
package.
