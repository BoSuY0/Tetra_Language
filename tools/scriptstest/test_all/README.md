# test_all script tests

Boundary: tests for `scripts/ci/test-all.sh`, its fixture repo, summary shape,
report-dir hygiene, and v0.5/v0.6 gate wiring used by the test-all workflow.

Keep broader release-version-specific checks in their dedicated sibling
directories.
