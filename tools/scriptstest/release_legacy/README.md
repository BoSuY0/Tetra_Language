# Legacy Release Gate Tests

This package owns tests for legacy pre-v0.3 release gate scripts:

- v0.1.1
- v0.1.2
- v0.1.3
- v0.2.0

Keep new release gate test coverage in the package that matches the current
release structure. This directory is only for the legacy gates listed above.
