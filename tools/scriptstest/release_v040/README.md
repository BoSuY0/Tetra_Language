# v0.4.0 Release Gate Script Tests

This directory owns Go tests for the v0.4.0 release gate scripts.

The package verifies the dedicated release gate and smoke scripts under
`scripts/release/v0_4_0`.
