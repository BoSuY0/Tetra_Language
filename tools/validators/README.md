# tools/validators

Shared validator contracts and reusable validation helpers belong here after
three or more `tools/cmd/validate-*` commands prove the same helper boundary.

Do not prematurely couple unrelated validators.

