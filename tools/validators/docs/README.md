# tools/validators/docs

Reusable documentation validator helpers belong here after multiple
`tools/cmd/validate-*` or `verify-docs` checks prove the same boundary.

Command-specific validation stays in `tools/cmd`.
