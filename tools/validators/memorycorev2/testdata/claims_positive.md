# Memory Core v2 Claims Fixture

Memory Core v2 supports canonical memory state in normal Linux-x64 builds with
evidence-bound island routes, backend events, and optimizer proof IDs.

Nonclaims:

- no universal memory safety claim
- no universal performance claim
- no zero heap for all programs claim
- no all-target backend runtime claim
