# tools/validators/eco

Reusable eco packaging and publishing validator helpers belong here after the
CLI and `tools/cmd/validate-eco-*` commands share the same contract.

Keep command entrypoints in `tools/cmd`.
