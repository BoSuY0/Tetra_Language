func main() -> Int
uses alloc, islands, mem:
    var total: Int = 0
    island(128) as isl:
        var xs: []i32 = core.island_make_i32(isl, 3)
        xs[0] = 10
        xs[1] = 20
        xs[2] = 12
        for x in xs:
            total = total + x
    return total
