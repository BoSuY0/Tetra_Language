// v0.5 stable core stdlib smoke.
module examples.core.data.core_math_smoke

import lib.core.math as math

func main() -> Int:
    let base: Int = math.min_i32(40, 99)
    let bonus: Int = math.max_i32(1, 2)
    let bounded: Int = math.clamp_i32(base, 0, 40)
    return math.add_i32(bounded, bonus)
