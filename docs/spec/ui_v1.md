# UI v1 Surface

Status: historical alias. The active `v0.4.0` UI compiler-to-runtime
contract is documented in [ui_v0.4.0.md](./ui_v0.4.0.md) and uses
`tetra.ui.v0.4.0` metadata.

Older plan and release notes may still mention `tetra.ui.v1` as a bounded
preview label. New compiler output, validators, Web UI smoke, and Linux-x64
desktop runtime evidence must use the `tetra.ui.v0.4.0` contract.
