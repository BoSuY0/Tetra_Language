# docs/architecture

Architecture maps, ownership boundaries, and prompt-to-artifact checklists live
here.

Implementation plans remain in `docs/plans`.

- `project_structure.md` records the compatibility-wrapper migration structure.
- `no_wrapper_project_structure.md` records the direct final-state structure
  where legacy entrypoints are removed after callers migrate.
