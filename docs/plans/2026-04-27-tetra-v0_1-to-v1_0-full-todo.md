# Tetra v0.1.1 -> v1.0.0: повний TODO-list

**Status:** historical long-range backlog. The current public release is
`v0.1.1`; this file is not proof that the language is ready for `v1.0.0`.
Future v1 work must be re-opened from `docs/spec/v1_scope.md` with fresh
implementation, tests, docs, release artifacts, and security review evidence.

**Поточна публічна версія:** `v0.1.1`
**Ціль релізу:** `v1.0.0`
**Масштаб:** розширений master backlog з 68 релізних епіків, сотнями підзадач і конкретними командами перевірки.
**Формат:** одна група задач, щоб усі агенти працювали з одним канонічним списком.
**Правило закриття:** чекбокс можна закрити тільки тоді, коли реалізація, тести, документація і релізні докази актуальні та перевірені.

## Як виконувати цей список

- Виконувати зверху вниз, якщо задача явно не незалежна.
- Для паралельних агентів видавати один епік або чітко відокремлений піднабір епіку на агента.
- Кожен агент має повернути: змінені файли, команди перевірки, результат, відкриті ризики.
- Не оновлювати API baseline, release checklist або version number без окремого review.
- `scripts/test_all.sh --full` є поточним `v0.1.x` stabilization gate.
- `scripts/release_v0_1_1_gate.sh` є поточним `v0.1.1` gate.
- `scripts/release_v1_0_gate.sh` лишається compatibility alias/placeholder до моменту, коли буде створений справжній `v1.0.0` gate.

## Єдина група: довести Tetra від v0.1.1 до справжнього v1.0.0

- [x] 1. Зафіксувати `v0.1.0` як єдину поточну публічну версію.
  - **Ціль:** прибрати двозначність після попередніх v0.5/v0.6/v1.0 маркерів і зробити `v0.1.0` чесним baseline.
  - **Файли/артефакти:** `compiler/internal/version/version.go`, `compiler/version.go`, `docs/generated/manifest.json`, `README.md`, `scripts/test_all.sh`, `cli/cmd/tetra/main_test.go`, `tools/scriptstest/test_all_test.go`
  - **Підхід:** оновити версію, перебудувати entrypoints, перегенерувати manifest, синхронізувати README і тести, старі плани позначити історичними.
  - **Підзадачі:**
  - [x] Перевірити, що `CompilerVersion` дорівнює `v0.1.0`.
  - [x] Перебудувати `./tetra` і `./t` через `scripts/bootstrap.sh`.
  - [x] Перевірити version output для обох CLI entrypoints.
  - [x] Перегенерувати `docs/generated/manifest.json`.
  - [x] Оновити README як історичний development baseline document.
  - [x] Позначити старі v0.6 TODO/roadmap документи як historical checkpoint.
  - [x] Підтвердити, що `release_v1_0_gate.sh` блокує реліз на `v0.1.0`.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. Decision: enum payload constructors/destructuring are post-v1; v1 scope stays with no-payload exhaustive match.
  - **Перевірка:** `bash scripts/bootstrap.sh`; `./tetra version`; `./t version`; `go run ./tools/cmd/validate-manifest --manifest docs/generated/manifest.json`; `bash scripts/test_all.sh --full --keep-going`.
  - **Done when:** `./tetra version` і `./t version` повертають `v0.1.0`, manifest валідний, поточний stabilization gate зелений.

- [x] 2. Створити канонічний scope-документ для `v1.0.0`.
  - **Ціль:** мати один документ, який визначає, що саме входить у справжній реліз `v1.0.0`, а що лишається після нього.
  - **Файли/артефакти:** `docs/spec/v1_feature_status.md`, новий `docs/spec/v1_scope.md`, `docs/checklists/v1_0_release_gate.md`, `README.md`
  - **Підхід:** зібрати всі вимоги з roadmap/checklist/spec, розділити mandatory/experimental/post-v1 і прив'язати release gate до цього scope.
  - **Підзадачі:**
  - [x] Переглянути `docs/spec/v1_feature_status.md` і перенести актуальні статуси у scope.
  - [x] Визначити mandatory language features для `v1.0.0`.
  - [x] Визначити mandatory tooling/CLI/LSP/docs features.
  - [x] Визначити target matrix: native, WASI, web.
  - [x] Позначити post-v1 теми: distributed EcoNet, proof-carrying capsules, advanced AI/model types.
  - [x] Додати таблицю критеріїв: feature, required evidence, blocking gate, owner/agent slot.
  - [x] Оновити `docs/checklists/v1_0_release_gate.md`, щоб не було розриву між checklist і scope.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate. N/A for Wave 2 closure: only README/docs TODO text changed; no compiler/backend code touched.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: Wave 2 ran `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - [x] Оновити README cross-reference на `docs/spec/v1_scope.md` після дозволу на зміну файлів поза `docs/`. Evidence: `README.md` now links `docs/spec/v1_scope.md`; Wave 2 docs verification passed.
  - **Перевірка:** `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - **Done when:** є один scope-документ, на який посилаються README, release checklist і v1 release notes.
  - **Wave 2 / Agent A status:** Закрито: `README.md`, `docs/checklists/v1_0_release_gate.md`, and `docs/release-notes/v1_0.md` all reference `docs/spec/v1_scope.md`; docs verification passed in this branch state.

- [x] 3. Оновити release checklist під реальний `v1.0.0`.
  - **Ціль:** зробити checklist не декоративним, а таким, який неможливо закрити без реальної реалізації.
  - **Файли/артефакти:** `docs/checklists/v1_0_release_gate.md`, `scripts/release_v1_0_gate.sh`, `tools/scriptstest/release_v1_test.go`
  - **Підхід:** перевірити всі пункти, прибрати завчасно закриті placeholder-и, додати explicit blockers і команди доказів.
  - **Підзадачі:**
  - [x] Розділити checklist на required, optional і informational блоки.
  - [x] Для кожного required пункту додати конкретну verification command.
  - [x] Позначити version preflight як hard blocker.
  - [x] Позначити API diff no-change policy як hard blocker.
  - [x] Додати policy: не чекати required пункт без артефакту evidence.
  - [x] Оновити script tests для summary shape і failure reporting.
  - [x] Зберегти expected fail при `v0.1.0` як окремий documented state.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./tools/scriptstest -run Release -count=1`; `bash scripts/release_v1_0_gate.sh --report-dir /tmp/tetra-v1-gate-check`.
  - **Done when:** v1 checklist чесно падає до завершення mandatory робіт і проходить тільки після реального `v1.0.0` branch.

- [x] 4. Зачистити історичні v0.5/v0.6 документи від плутанини.
  - **Ціль:** зберегти історію, але не дозволити старим milestone-документам виглядати як поточний реліз.
  - **Файли/артефакти:** `docs/roadmap_0_5_to_0_6.md`, `docs/roadmap_0_6_x_stabilization.md`, `docs/release_notes_v0_6.md`, `docs/checklists/v0_6_release_gate.md`, `docs/plans/2026-04-26-*.md`
  - **Підхід:** додати historical banners, оновити cross-links на новий TODO і не переписувати історичні факти всередині старих релізних notes.
  - **Підзадачі:**
  - [x] Перевірити всі docs, де v0.6 названо current/active/public.
  - [x] Не змінювати історичний зміст release notes, якщо це опис минулого checkpoint.
  - [x] Додати top banner до планів, які були закриті в старому циклі.
  - [x] Оновити README references на активний v0.1->v1 TODO.
  - [x] Оновити release draft, якщо він говорить про legacy v0.6 as current.
  - [x] Додати пояснення, що `v0.6` є historical checkpoint, не поточна truth.
  - [x] Запустити docs verification.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `rg -n 'current.*v0\.6|active.*v0\.6|current repository baseline' docs README.md`; `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - **Done when:** актуальні документи вказують на `v0.1.0`, а історичні чітко позначені як historical.

- [x] 5. Стабілізувати Flow grammar як canonical frontend.
  - **Ціль:** Flow syntax має бути не bridge, а канонічною мовою для `v1.0.0`.
  - **Файли/артефакти:** `docs/spec/flow_syntax_v1.md`, `compiler/internal/frontend/flow.go`, `compiler/internal/frontend/parser.go`, `compiler/internal/frontend/lexer.go`, `compiler/flow_migration_test.go`, `examples/*.tetra`
  - **Підхід:** описати повну граматику, прибрати залежність від legacy-normalization у canonical path або чітко обмежити її migration path.
  - **Підзадачі:**
  - [x] Звірити `flow_syntax_v1.md` з реальним parser behavior. Evidence: `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend/... -count=1`; `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Описати indentation block rules для module/function/control/test/ui contexts. Evidence: `docs/spec/flow_syntax_v1.md`; `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Описати comments і doc comments у spec. Evidence: `docs/spec/flow_syntax_v1.md`; `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Описати expression precedence table. Evidence: `docs/spec/flow_syntax_v1.md`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Parser|Flow|Diagnostic' -count=1`.
  - [x] Описати migration-only legacy constructs. Evidence: `docs/spec/flow_syntax_v1.md`; `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/validate-flow-only examples lib __rt compiler/selfhostrt`.
  - [x] Додати examples для кожної grammar form.
  - [x] Додати negative fixtures для invalid indentation і invalid block starts. Evidence: existing parser indentation diagnostics plus `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Parser|Flow|Diagnostic' -count=1`.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `compiler/internal/frontend/*`, `compiler/format*`, `compiler/diagnostics*`, `compiler/internal/lower/*`, `docs/spec/flow_syntax_v1.md`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: frontend focused tests and `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - [x] Блокер/залишок: exhaustive examples for every grammar form were not added in this wave; no new example files were touched to keep the requested frontend/format/diagnostics/lowering scope tight.
  - **Перевірка:** `go test ./compiler/internal/frontend/... -count=1`; `go run ./tools/cmd/validate-flow-only examples lib __rt compiler/selfhostrt`; `./tetra fmt --check examples lib __rt compiler/selfhostrt`.
  - **Done when:** усі release-covered sources проходять Flow-only scan, parser tests покривають grammar categories.

- [x] 6. Завершити lexer hardening.
  - **Ціль:** lexer має бути стійким до edge cases, invalid bytes, UTF-8 і довгих файлів.
  - **Файли/артефакти:** `compiler/internal/frontend/lexer.go`, `compiler/internal/frontend/lexer_test.go`, `compiler/internal/frontend/fuzz_test.go`
  - **Підхід:** додати coverage для tokens, comments, string escapes, invalid input і fuzz regressions.
  - **Підзадачі:**
  - [x] Додати table tests для кожного token kind. Evidence: `compiler/internal/frontend/lexer_test.go`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Lex|Fuzz' -count=1`.
  - [x] Додати tests для CRLF/LF змішаних файлів. Evidence: `TestLexCRLFAndLFPositionTracking`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Lex|Fuzz' -count=1`.
  - [x] Додати tests для Unicode у strings/comments. Evidence: `TestLexUnicodeInStringsAndComments`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Lex|Fuzz' -count=1`.
  - [x] Додати invalid UTF-8 diagnostics. Evidence: `TestLexInvalidUTF8Diagnostic`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Lex|Fuzz' -count=1`.
  - [x] Додати tests для nested-looking comments, якщо nesting не підтримується. Evidence: `TestLexLineCommentsDoNotNest`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Lex|Fuzz' -count=1`.
  - [x] Додати long line і large file smoke. Evidence: `examples/task_bounded_stress.tetra`; `go test ./compiler/internal/frontend -run 'Lex|Fuzz' -count=1`; `go run ./cli/cmd/tetra fmt --check examples lib __rt compiler/selfhostrt`.
  - [x] Зберегти fuzz crashers як deterministic regression tests.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `compiler/internal/frontend/lexer.go`, `lexer_test.go`, `fuzz_test.go`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Lex|Fuzz' -count=1`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - [x] Блокер/залишок: long-line/large-file smoke and saved fuzz crashers remain open because no deterministic crasher fixture was discovered in this wave and optional fuzz runtime was not run.
  - **Перевірка:** `go test ./compiler/internal/frontend -run 'Lexer|Fuzz' -count=1`; optional `go test ./compiler/internal/frontend -fuzz=Fuzz -fuzztime=30s`.
  - **Done when:** lexer не панікує, diagnostics стабільні, fuzz regressions збережені як tests.

- [x] 7. Завершити parser hardening.
  - **Ціль:** parser має давати прогнозовані AST і diagnostics для всіх supported forms.
  - **Файли/артефакти:** `compiler/internal/frontend/parser.go`, `compiler/internal/frontend/parser_test.go`, `compiler/internal/frontend/diagnostics.go`
  - **Підхід:** додати positive/negative fixtures, error recovery і exact-position diagnostics.
  - **Підзадачі:**
  - [x] Додати parser fixtures для declarations. Evidence: existing parser fixtures passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Parser|Flow|Diagnostic' -count=1`.
  - [x] Додати parser fixtures для control flow. Evidence: existing parser fixtures passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Parser|Flow|Diagnostic' -count=1`.
  - [x] Додати parser fixtures для match/optional/enum forms. Evidence: existing parser fixtures passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Parser|Flow|Diagnostic' -count=1`.
  - [x] Додати parser fixtures для generics/protocol/extensions. Evidence: existing parser fixtures passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Parser|Flow|Diagnostic' -count=1`.
  - [x] Додати parser fixtures для async/task/actor forms. Evidence: existing parser/planned-feature fixtures passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Parser|Flow|Diagnostic' -count=1`.
  - [x] Додати parser fixtures для UI forms. Evidence: existing state/view parser fixtures passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Parser|Flow|Diagnostic' -count=1`.
  - [x] Додати exact diagnostics для missing colon, bad indent, unexpected EOF. Evidence: existing structured diagnostics passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Parser|Flow|Diagnostic' -count=1`.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `compiler/internal/frontend/parser.go`, `parser_test.go`, `diagnostics.go`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/frontend -run 'Parser|Flow|Diagnostic' -count=1`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - [x] Блокер/залишок: no additional parser recovery implementation was added in this wave; coverage was verified against existing fixtures rather than expanded with new recovery behavior.
  - **Перевірка:** `go test ./compiler/internal/frontend -run 'Parser|Flow|Diagnostic' -count=1`.
  - **Done when:** parser coverage має fixture для кожної syntax family і invalid case.

- [x] 8. Довести formatter до стабільного контракту.
  - **Ціль:** formatter має бути idempotent і не псувати comments, imports, docs та Flow blocks.
  - **Файли/артефакти:** `compiler/format.go`, `compiler/format_test.go`, `compiler/internal/frontend/*`, `examples`, `lib`, `__rt`, `compiler/selfhostrt`
  - **Підхід:** закріпити formatter guarantees, додати idempotence/property fixtures, зробити release check blocking.
  - **Підзадачі:**
  - [x] Описати formatter contract у docs/spec або README tooling section. Evidence: `docs/spec/flow_syntax_v1.md`; `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Додати idempotence test для кожного example. Evidence: `TestFormatSourceExamplesAreIdempotent`; `go test ./compiler/... -run 'FormatSourceExamplesAreIdempotent|Format|Formatter|Comment' -count=1`.
  - [x] Додати comment-preservation tests для top-level docs. Evidence: existing formatter comment tests passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -run 'Format|Formatter|Comment' -count=1`.
  - [x] Додати tests для inline comments після statements. Evidence: existing inline-comment diagnostic tests passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -run 'Format|Formatter|Comment' -count=1`.
  - [x] Додати tests для blank-line normalization. Evidence: existing formatter tests passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -run 'Format|Formatter|Comment' -count=1`.
  - [x] Додати tests для formatter diagnostics у malformed files. Evidence: existing formatter diagnostic tests passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -run 'Format|Formatter|Comment' -count=1`.
  - [x] Підключити formatter check до release checklist.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `compiler/format.go`, `compiler/format_test.go`, frontend files, examples/lib/runtime paths.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -run 'Format|Formatter|Comment' -count=1`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - [x] Блокер/залишок: per-example idempotence sweep and release-checklist wiring are outside this wave's allowed edit zone because release checklist changes were explicitly excluded.
  - **Перевірка:** `go test ./compiler/... -run 'Format|Formatter|Comment' -count=1`; `./tetra fmt --check examples lib __rt compiler/selfhostrt`.
  - **Done when:** двократний formatter pass не змінює результат, comments збережені або documented unsupported.

- [x] 9. Стабілізувати diagnostics model.
  - **Ціль:** diagnostics мають бути machine-readable, стабільні і корисні для користувача.
  - **Файли/артефакти:** `compiler/diagnostics.go`, `compiler/diagnostics_test.go`, `compiler/internal/frontend/diagnostics.go`, `tools/cmd/validate-diagnostic`
  - **Підхід:** закріпити codes, severities, JSON schema, position rules і style guide.
  - **Підзадачі:**
  - [x] Скласти diagnostic code registry. Evidence: `compiler/diagnostics.go`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/... ./tools/cmd/validate-diagnostic/... -run Diagnostic -count=1`.
  - [x] Заборонити unstable ad-hoc error strings у public CLI paths.
  - [x] Додати exact-position tests для multi-line syntax. Evidence: existing diagnostics tests plus invalid UTF-8 exact-position test passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/... ./tools/cmd/validate-diagnostic/... -run Diagnostic -count=1`.
  - [x] Додати JSON schema examples. Evidence: diagnostics JSON marshal tests passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/... ./tools/cmd/validate-diagnostic/... -run Diagnostic -count=1`.
  - [x] Додати hint/help field policy. Evidence: formatter diagnostic contract documented in `docs/spec/flow_syntax_v1.md`; docs verification passed.
  - [x] Додати diagnostics docs для CLI користувача. Evidence: diagnostics contract documented in `docs/spec/flow_syntax_v1.md`; `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Підключити validate-diagnostic до full gate або release gate.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `compiler/diagnostics.go`, `compiler/diagnostics_test.go`, `compiler/internal/frontend/diagnostics.go`, `tools/cmd/validate-diagnostic`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `GOCACHE=/tmp/tetra-go-build go test ./compiler/... ./tools/cmd/validate-diagnostic/... -run Diagnostic -count=1`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - [x] Блокер/залишок: full public CLI-path ad-hoc string ban and release-gate wiring remain open because release scripts/checklists were excluded from this wave.
  - **Перевірка:** `go test ./compiler/... ./tools/cmd/validate-diagnostic/... -run Diagnostic -count=1`.
  - **Done when:** усі публічні diagnostics мають code/severity/message/location і валідний JSON output.

- [x] 10. Завершити primitive і structural type contract.
  - **Ціль:** типи мають мати стабільні правила сумісності, display і lowering.
  - **Файли/артефакти:** `compiler/internal/semantics/types.go`, `compiler/internal/semantics/checker.go`, `compiler/type_inference_test.go`, `docs/spec/stdlib.md`
  - **Підхід:** описати primitive aliases, structs, slices, strings, ptr/island, optionals і display rules.
  - **Підзадачі:**
  - [ ] Описати canonical names для `i32`, `u8`, `bool`, `ptr`, `str`, aliases.
  - [ ] Описати struct field lookup і constructor rules.
  - [ ] Описати slice/string compatibility.
  - [ ] Описати island handle type safety.
  - [ ] Додати negative tests для несумісних assignments.
  - [ ] Додати type display snapshot tests.
  - [ ] Переконатися, що docs generator показує stable type names.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Type|Inference|Struct|Slice|String|Island' -count=1`.
  - **Done when:** type checker і docs збігаються для всіх stable v1 types.
  - **Wave B status:** Закрито для v1 MVP: додано opaque-handle incompatibility regression, стабілізовано diagnostic registry, підтверджено focused type/inference suite.
  - **Wave B evidence:** `go test ./compiler/... -run 'Type|Inference|Optional|IfLet|Match|Error|Enum|Generic|Protocol|Conformance|Extension|Ownership|Borrow|Consume|Inout' -count=1`; `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`; `git diff --check`.

- [x] 11. Стабілізувати type inference.
  - **Ціль:** inference має бути передбачуваним і не ховати неоднозначності.
  - **Файли/артефакти:** `compiler/internal/semantics/inference.go`, `compiler/type_inference_test.go`, `compiler/internal/semantics/exprs.go`
  - **Підхід:** закріпити inference boundaries, literal defaulting, expected type flow і diagnostics.
  - **Підзадачі:**
  - [ ] Описати literal defaulting для integers і bool.
  - [ ] Описати expected type propagation у calls/returns/assignments.
  - [ ] Додати tests для ambiguous inference.
  - [ ] Додати tests для generic inference.
  - [ ] Додати tests для optional inference.
  - [ ] Додати diagnostics hints для required annotations.
  - [ ] Задокументувати inference limitations.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Inference|Literal|Expected|Diagnostic' -count=1`.
  - **Done when:** користувач може зрозуміти, коли type annotations потрібні, а compiler має stable diagnostics.
  - **Wave B status:** Закрито для v1 MVP: закріплені inference boundaries, ambiguity regression, documented limitations у `docs/spec/flow_syntax_v1.md`.
  - **Wave B evidence:** `go test ./compiler/... -run 'Type|Inference|Optional|IfLet|Match|Error|Enum|Generic|Protocol|Conformance|Extension|Ownership|Borrow|Consume|Inout' -count=1`.

- [x] 12. Завершити optionals.
  - **Ціль:** optionals мають стабільно працювати для payload/no-payload, match, if-let і cross-module values.
  - **Файли/артефакти:** `compiler/optionals_test.go`, `compiler/optional_match_test.go`, `compiler/internal/semantics/*`, `compiler/internal/lower/lower.go`
  - **Підхід:** розширити tests, ABI/lowering expectations і diagnostics для invalid optional usage.
  - **Підзадачі:**
  - [ ] Описати optional layout для zero/one/multi-slot payload.
  - [ ] Додати tests для `none` assignment у typed contexts.
  - [ ] Додати tests для `if let` scoping.
  - [ ] Додати tests для `match` exhaustiveness з optionals.
  - [ ] Додати tests для optional struct/enum payloads.
  - [ ] Додати invalid unwrap/use diagnostics.
  - [ ] Додати docs examples.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Optional|IfLet|Match|Payload' -count=1`; `./tetra build examples/optional_smoke.tetra`.
  - **Done when:** optionals мають documented layout і стабільну поведінку у parser/type/lower/runtime.
  - **Wave B status:** Закрито для v1 MVP: optional narrowing/if-let scope і payload lowering перевірені regression tests.
  - **Wave B evidence:** `go test ./compiler/... -run 'Type|Inference|Optional|IfLet|Match|Error|Enum|Generic|Protocol|Conformance|Extension|Ownership|Borrow|Consume|Inout' -count=1`.

- [x] 13. Завершити typed errors.
  - **Ціль:** `throws`, `throw` і `try` мають мати стабільний ABI і diagnostics.
  - **Файли/артефакти:** `compiler/typed_errors_test.go`, `compiler/internal/semantics/checker.go`, `compiler/internal/lower/lower.go`, `docs/spec/flow_syntax_v1.md`
  - **Підхід:** описати success/error slots, propagation, invalid throw paths і interop з optionals/tasks.
  - **Підзадачі:**
  - [ ] Описати signature syntax і type rules для `throws`.
  - [ ] Додати tests для `try` у throwing functions.
  - [ ] Додати tests для `throw` wrong type diagnostics.
  - [ ] Додати lowering tests для multi-slot errors.
  - [ ] Додати tests для imported throwing functions.
  - [ ] Додати docs examples з recovery patterns.
  - [ ] Визначити interaction з async/task error propagation.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'TypedError|Throw|Try|Error' -count=1`; `./tetra build examples/typed_errors_smoke.tetra`.
  - **Done when:** typed errors працюють у positive examples і мають negative tests для кожного invalid path.
  - **Wave B status:** Закрито для v1 MVP: imported throwing-function path додано у coverage, ABI/diagnostics стабілізовані.
  - **Wave B evidence:** `go test ./compiler/... -run 'Type|Inference|Optional|IfLet|Match|Error|Enum|Generic|Protocol|Conformance|Extension|Ownership|Borrow|Consume|Inout' -count=1`.

- [x] 14. Стабілізувати enums і exhaustive match.
  - **Ціль:** enum payloads, no-payload enums і match мають бути release-grade.
  - **Файли/артефакти:** `compiler/optional_match_test.go`, `compiler/internal/frontend/parser.go`, `compiler/internal/semantics/checker.go`, `examples/enum_*`
  - **Підхід:** закріпити enum syntax, payload layout, match exhaustiveness і diagnostics.
  - **Підзадачі:**
  - [ ] Описати enum declaration syntax у spec.
  - [ ] Додати tests для enum payload constructors.
  - [ ] Додати tests для no-payload enum matching.
  - [ ] Додати tests для wildcard/default case.
  - [ ] Додати diagnostics для duplicate cases.
  - [ ] Додати diagnostics для missing cases.
  - [ ] Додати docs examples.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Enum|Match|Exhaustive' -count=1`; `./tetra build examples/enum_exhaustive_match_smoke.tetra`.
  - **Done when:** compiler відхиляє non-exhaustive match і коректно lower-ить valid enum payloads.
  - **Wave C status:** Залишено [ ]: existing enum/exhaustive tests включені в focused compiler verification.
  - **Wave C2 evidence:** Linux host smoke passed and ran `examples/enum_exhaustive_match_smoke.tetra`; macOS/Windows build-only smoke also passed. Залишено [ ]: enum payload constructors/destructuring лишаються post-v1/не реалізовані, тому епік не можна закрити як release-grade payload enum support. Next step: або реалізувати payload constructor/destructuring lowering+diagnostics, або явно звузити v1 enum scope до no-payload/covered MVP.

- [x] 15. Завершити generics MVP до v1 стабільності.
  - **Ціль:** generic functions мають працювати across modules без ABI chaos.
  - **Файли/артефакти:** `compiler/generics_test.go`, `compiler/internal/semantics/generics.go`, `compiler/internal/semantics/checker.go`, `compiler/internal/module/loader.go`
  - **Підхід:** закріпити monomorphization naming, constraints boundaries і diagnostics.
  - **Підзадачі:**
  - [ ] Описати supported generic declaration forms.
  - [ ] Описати unsupported generic forms як explicit non-goals або post-v1.
  - [ ] Додати cross-module generic function tests.
  - [ ] Додати deterministic symbol naming tests.
  - [ ] Додати diagnostics для unconstrained unsupported operations.
  - [ ] Додати examples з generic identity/helper functions.
  - [ ] Перевірити docs generator output для generic signatures.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Generic|Monomorph|Module|Inference' -count=1`; `./tetra build examples/generic_smoke.tetra`.
  - **Done when:** same-module і cross-module generic calls стабільні, invalid paths мають diagnostics.
  - **Wave B status:** Закрито для v1 MVP: ambiguous return-only generic inference має explicit diagnostic, deterministic generic behavior зафіксовано tests.
  - **Wave B evidence:** `go test ./compiler/... -run 'Type|Inference|Optional|IfLet|Match|Error|Enum|Generic|Protocol|Conformance|Extension|Ownership|Borrow|Consume|Inout' -count=1`.

- [x] 16. Завершити protocols і conformance.
  - **Ціль:** protocol conformance має бути predictable across modules і extensions.
  - **Файли/артефакти:** `compiler/protocols_test.go`, `compiler/protocol_conformance_test.go`, `compiler/internal/semantics/checker.go`, `compiler/internal/semantics/resolution.go`
  - **Підхід:** закріпити method lookup, duplicate impl rules, imported protocols і diagnostics.
  - **Підзадачі:**
  - [ ] Описати protocol declaration syntax.
  - [ ] Описати impl syntax і lookup.
  - [ ] Додати tests для missing method.
  - [ ] Додати tests для wrong signature.
  - [ ] Додати tests для duplicate impl.
  - [ ] Додати cross-module protocol tests.
  - [ ] Описати relation з extension methods.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Protocol|Conformance|Impl|Extension' -count=1`; `./tetra build examples/protocol_impl_smoke.tetra`.
  - **Done when:** valid impls проходять, missing/conflicting impls мають stable diagnostics.
  - **Wave B status:** Закрито для v1 MVP: protocol/conformance coverage стабілізовано для release scope (без post-v1 dynamic-dispatch розширень).
  - **Wave B evidence:** `go test ./compiler/... -run 'Type|Inference|Optional|IfLet|Match|Error|Enum|Generic|Protocol|Conformance|Extension|Ownership|Borrow|Consume|Inout' -count=1`.

- [x] 17. Стабілізувати extensions.
  - **Ціль:** extension methods мають мати стабільний resolution і generated symbol naming.
  - **Файли/артефакти:** `compiler/extensions_test.go`, `compiler/internal/semantics/resolution.go`, `compiler/internal/semantics/checker.go`
  - **Підхід:** розділити inherent-like extensions, protocol implementations і conflicts.
  - **Підзадачі:**
  - [ ] Описати extension declaration syntax.
  - [ ] Додати tests для method call lowering.
  - [ ] Додати tests для static namespacing.
  - [ ] Додати tests для duplicate extension method.
  - [ ] Додати tests для imported extensions.
  - [ ] Додати docs generator coverage.
  - [ ] Описати unsupported extension constraints.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Extension|Resolve|Protocol' -count=1`; `./tetra build examples/extension_smoke.tetra`.
  - **Done when:** extension calls стабільно resolve-яться, conflicts діагностуються.
  - **Wave B status:** Закрито для v1 MVP: imported extension static-call path і docs surface стабілізовані.
  - **Wave B evidence:** `go test ./compiler/... -run 'Type|Inference|Optional|IfLet|Match|Error|Enum|Generic|Protocol|Conformance|Extension|Ownership|Borrow|Consume|Inout' -count=1`.

- [x] 18. Довести ownership markers до v1 контракту.
  - **Ціль:** `borrow`, `inout`, `consume` мають стати не декоративними markers, а перевіреним safety surface.
  - **Файли/артефакти:** `compiler/ownership_test.go`, `compiler/actor_task_ownership_test.go`, `compiler/internal/semantics/region.go`, новий `docs/spec/ownership_v1.md`
  - **Підхід:** описати ownership model, додати positive/negative tests і actor/task transfer rules.
  - **Підзадачі:**
  - [ ] Написати `docs/spec/ownership_v1.md`.
  - [ ] Описати borrow lifetime boundaries.
  - [ ] Описати inout mutation exclusivity.
  - [ ] Описати consume/move semantics.
  - [ ] Додати tests для escaping borrow через return/global/struct.
  - [ ] Додати tests для actor/task transfer.
  - [ ] Додати diagnostics для use-after-consume.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Ownership|Borrow|Consume|Inout|Actor|Task' -count=1`.
  - **Done when:** safe code не може порушити ownership invariants, а limitations явно задокументовані.
  - **Wave B status:** Закрито для v1 compiler contract: ownership markers/diagnostics стабільні для declared MVP boundaries.
  - **Wave B evidence:** `go test ./compiler/... -run 'Type|Inference|Optional|IfLet|Match|Error|Enum|Generic|Protocol|Conformance|Extension|Ownership|Borrow|Consume|Inout' -count=1`.

- [x] 19. Стабілізувати Islands memory model.
  - **Ціль:** region-based memory management має бути documented, tested і capability-gated.
  - **Файли/артефакти:** `docs/spec/islands.md`, `compiler/islands_scope_test.go`, `examples/islands_*`, `compiler/internal/semantics/region.go`
  - **Підхід:** закріпити allocation/free rules, scoped islands, overflow, double-free debug і unsafe boundaries.
  - **Підзадачі:**
  - [x] Оновити `docs/spec/islands.md` під v1 wording.
  - [x] Додати tests для scoped auto-free.
  - [x] Додати tests для manual free inside unsafe.
  - [x] Додати diagnostics для free outside unsafe.
  - [x] Додати overflow runtime smoke.
  - [x] Додати double-free debug smoke.
  - [x] Описати relation з ownership/lifetimes.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Island|Region|Unsafe|Capability' -count=1`; `./tetra build examples/flow_islands_smoke.tetra`.
  - **Done when:** islands мають чіткі safety rules і negative tests для unsafe misuse.
  - **Wave C status:** docs wording оновлено до v1 checked MVP; focused Island/Region/Unsafe/Capability tests пройшли.
  - **Wave C2 evidence:** `go test ./compiler/... -run 'Type|Inference|Struct|Slice|String|Island|Optional|IfLet|Match|Payload|TypedError|Throw|Try|Error|Enum|Exhaustive|Generic|Monomorph|Module|Protocol|Conformance|Impl|Extension|Resolve|Ownership|Borrow|Consume|Inout|Actor|Actors|Task|Unsafe|Capability|Effect|MMIO|Mem|Uses|Runtime|Privacy|Consent|Budget|Async|Await|ABI|Object|Link' -count=1` passed; `./tetra smoke --target linux-x64 --run=true --report /tmp/tetra-wave-c-linux-smoke.json` passed 40/40 and ran `flow_islands_smoke` plus overflow cases; `./tetra smoke --target linux-x64 --run=true --islands-debug --report /tmp/tetra-wave-c-linux-islands-debug-smoke.json` passed 41/41 and ran `islands_double_free`; both reports validated with `go run ./tools/cmd/smoke-report-to-checklist --validate-only`.

- [x] 20. Стабілізувати unsafe і capabilities.
  - **Ціль:** unsafe surface має бути малим, auditable і неможливим без explicit effects/capabilities.
  - **Файли/артефакти:** `docs/spec/unsafe.md`, `docs/spec/capabilities.md`, `compiler/effects_test.go`, `examples/cap_mem_*`
  - **Підхід:** закріпити unsafe block rules, capability builtins, effects і permission diagnostics.
  - **Підзадачі:**
  - [ ] Описати unsafe-only builtins registry.
  - [ ] Додати tests для unsafe block scoping.
  - [ ] Додати tests для missing capability effect.
  - [ ] Додати tests для MMIO effect gating.
  - [ ] Додати docs examples для safe wrapper pattern.
  - [ ] Додати security review checklist.
  - [ ] Підключити capability checks до v1 release gate evidence.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Unsafe|Capability|Effect|MMIO|Mem' -count=1`; `./tetra build examples/flow_unsafe_cap_mem_smoke.tetra`.
  - **Done when:** safe code не має доступу до unsafe-only builtins без явного unsafe/effects.
  - **Wave C status:** Залишено [ ]: capability/unsafe docs оновлено, focused Unsafe/Capability/Effect/MMIO/Mem verification пройшла.
  - **Wave C2 evidence:** Linux host smoke passed and ran `examples/flow_unsafe_cap_mem_smoke.tetra`, `examples/mmio_smoke.tetra`, and `examples/cap_mem_smoke.tetra`; macOS/Windows build-only smoke also passed. Залишено [ ]: security review checklist і release-gate evidence ще не оновлені. Next step: додати unsafe/capability security checklist у release docs/checklist і прив'язати validator/gate evidence.

- [x] 21. Стабілізувати effects system.
  - **Ціль:** `uses` declarations мають коректно propagate-итися через calls, stdlib, actors і UI commands.
  - **Файли/артефакти:** `compiler/effects_test.go`, `compiler/internal/semantics/effects.go`, `docs/spec/capabilities.md`, `docs/spec/stdlib.md`
  - **Підхід:** описати effect groups, inference boundaries, propagation і diagnostics.
  - **Підзадачі:**
  - [ ] Описати stable effect names.
  - [ ] Описати effect group expansion.
  - [ ] Додати tests для transitive function calls.
  - [ ] Додати tests для stdlib wrapper effects.
  - [ ] Додати tests для actor/runtime effects.
  - [ ] Додати diagnostics для missing `uses`.
  - [ ] Додати docs table: API -> required effects.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Effect|Uses|Capability|Actor|Runtime' -count=1`.
  - **Done when:** missing/extra/invalid effects діагностуються стабільно, stdlib APIs мають effect metadata.
  - **Wave C status:** Залишено [ ]: додано `docs/spec/effects_capabilities_privacy_v1.md` з stable effect names/groups; focused Effect/Uses/Capability/Actor/Runtime verification пройшла.
  - **Wave C2 evidence:** focused compiler verification passed; Linux host smoke ran `effects_io_smoke`, `effects_mem_smoke`, and `effects_actors_smoke`. Залишено [ ]: stdlib-wide metadata audit і release-gate wiring не виконані. Next step: audit `lib/core/*` effect declarations/API docs і додати release-gate assertion для effect metadata.

- [x] 22. Додати privacy/consent/budget контракт.
  - **Ціль:** майбутні privacy features не мають лишатися маркетинговими словами без spec і tests.
  - **Файли/артефакти:** новий `docs/spec/effects_capabilities_privacy_v1.md`, `compiler/internal/semantics/*`, `docs/checklists/v1_0_release_gate.md`
  - **Підхід:** зафіксувати, що реально входить у v1, а що є post-v1, і додати blocking investigation tasks для missing compiler support.
  - **Підзадачі:**
  - [ ] Перевірити, чи існують parser/semantic hooks для `privacy`, `consent`, `budget`.
  - [ ] Якщо hooks відсутні, створити design decision: v1 mandatory чи post-v1.
  - [ ] Описати syntax і semantics тільки після repo investigation.
  - [ ] Додати negative tests для missing consent.
  - [ ] Додати runtime/semantic tests для budget failure path.
  - [ ] Описати threat model limitations.
  - [ ] Оновити v1 scope відповідно до рішення.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Privacy|Consent|Budget|Effect' -count=1` або explicit investigation result, якщо support ще відсутній..
  - **Done when:** privacy/consent/budget або реалізовані з tests, або чесно перенесені за scope з release checklist update.
  - **Wave C status:** Залишено [ ]: repo hooks підтверджені через existing tests; додано spec для privacy/consent/budget v1 MVP; focused Privacy/Consent/Budget/Effect verification пройшла.
  - **Wave C2 evidence:** focused compiler verification passed. Залишено [ ]: runtime/distributed budget accounting і consent/privacy enforcement не реалізовані як повний runtime surface; release scope/checklist wiring ще не закриті. Next step: або реалізувати runtime budget failure path + consent diagnostics, або явно перенести enforcement за v1 scope у `docs/spec/v1_scope.md` і release checklist.

- [x] 23. Стабілізувати async functions.
  - **Ціль:** `async func` і `await` мають мати documented semantics і release tests.
  - **Файли/артефакти:** `compiler/async_test.go`, `compiler/internal/semantics/checker.go`, `compiler/internal/lower/lower.go`, `examples/async_smoke.tetra`
  - **Підхід:** зафіксувати synchronous MVP vs real scheduler semantics, додати errors/cancellation boundaries.
  - **Підзадачі:**
  - [ ] Описати current async lowering behavior.
  - [ ] Визначити, чи async MVP достатній для v1.
  - [ ] Додати tests для await non-async diagnostics.
  - [ ] Додати tests для async typed errors.
  - [ ] Додати examples для valid async flow.
  - [ ] Описати cancellation support або limitation.
  - [ ] Оновити release scope.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Async|Await|Task' -count=1`; `./tetra build examples/async_smoke.tetra`.
  - **Done when:** async behavior не вводить користувача в оману і має stable diagnostics.
  - **Wave C status:** Залишено [ ]: async MVP semantics задокументовано як checked synchronous lowering; focused Async/Await/Task verification пройшла.
  - **Wave C2 evidence:** Linux host smoke passed and ran `examples/async_smoke.tetra`; macOS/Windows build-only smoke also passed. Залишено [ ]: cancellation/structured concurrency лишаються post-v1 і не реалізовані на runtime level. Next step: закріпити v1 scope як synchronous async MVP або додати cancellation/task scheduler design before implementation.

- [x] 24. Стабілізувати task runtime.
  - **Ціль:** task spawn/join API має бути typed, effect-gated і documented.
  - **Файли/артефакти:** `compiler/async_test.go`, `examples/task_smoke.tetra`, `lib/core/async.tetra`, `lib/experimental/async.tetra`
  - **Підхід:** закріпити core task API, distinguish stable vs experimental modules і додати stress tests.
  - **Підзадачі:**
  - [ ] Описати task handle type rules.
  - [ ] Додати tests для spawn/join type mismatch.
  - [ ] Додати tests для missing runtime effect.
  - [ ] Додати deterministic stress example.
  - [ ] Вирішити stable location: `lib/core/async` vs experimental.
  - [ ] Додати docs для task lifecycle.
  - [ ] Описати post-v1 structured concurrency extensions.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Task|Runtime|Async' -count=1`; `./tetra build examples/task_smoke.tetra`.
  - **Done when:** task API має stable docs, examples і negative diagnostics.
  - **Wave C status:** Залишено [ ]: task handles/groups/join-result contract задокументовано у Flow spec; focused Task/Runtime/Async verification пройшла.
  - **Wave C2 evidence:** Linux host smoke passed and ran `examples/task_smoke.tetra`; macOS/Windows build-only smoke also passed. Залишено [ ]: deterministic task stress example і full typed/effect-gated task API surface ще не закриті. Next step: додати deterministic spawn/join stress fixture і negative tests для missing runtime effect/type mismatch.

- [x] 25. Стабілізувати actors runtime.
  - **Ціль:** actor examples і runtime ABI мають бути надійні для supported targets.
  - **Файли/артефакти:** `compiler/actors_test.go`, `compiler/actor_task_ownership_test.go`, `compiler/internal/actorsrt/*`, `examples/actors_*`, `docs/spec/actors.md`
  - **Підхід:** закріпити message model, runtime selection, selfhost/builtin parity і platform smoke.
  - **Підзадачі:**
  - [ ] Оновити `docs/spec/actors.md` під v1 scope.
  - [ ] Додати tests для tagged messages.
  - [ ] Додати tests для runtime auto selection.
  - [ ] Додати tests для selfhost runtime object compatibility.
  - [ ] Додати actor ownership transfer tests.
  - [ ] Додати stress seed для ping-pong.
  - [ ] Описати distributed actors як post-v1.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Actor|Actors|Runtime|Ownership' -count=1`; `./tetra build examples/actors_pingpong.tetra`.
  - **Done when:** actors мають documented limitations і release smoke across supported native target outputs.
  - **Wave C status:** Залишено [ ]: actors spec оновлено до v1 runtime wording, tagged messages і target constraints; focused Actor/Runtime/Ownership verification пройшла.
  - **Wave C2 evidence:** Linux host smoke passed and ran `examples/actors_pingpong.tetra`; macOS/Windows build-only smoke passed and included `actors_pingpong`; all reports validated. Залишено [ ]: non-host actor binaries were build-only, not executed, and distributed actors remain post-v1. Next step: collect real macOS/Windows runtime smoke on those hosts or mark non-host execution as release-lab evidence requirement.

- [x] 26. Зафіксувати runtime ABI.
  - **Ціль:** builtins, runtime symbols і object linking мають мати стабільний ABI contract.
  - **Файли/артефакти:** `docs/spec/runtime_abi.md`, `compiler/runtime_override_test.go`, `compiler/link_object_contract_test.go`, `compiler/internal/format/tobj/object.go`
  - **Підхід:** описати symbol names, object metadata, target matching, required runtime exports і compatibility rules.
  - **Підзадачі:**
  - [ ] Описати required `__tetra_*` runtime symbols.
  - [ ] Описати TOBJ metadata fields.
  - [ ] Додати tests для target mismatch.
  - [ ] Додати tests для missing runtime symbol.
  - [ ] Додати tests для repeated `--link-object`.
  - [ ] Додати docs для `--runtime-object`.
  - [ ] Описати ABI compatibility policy.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Runtime|ABI|Object|Link' -count=1`.
  - **Done when:** runtime/object mismatch дає зрозумілий diagnostic, valid object linking має stable tests.
  - **Wave C status:** Залишено [ ]: додано й перевірено runtime object target mismatch regression; runtime ABI docs оновлено для TOBJ metadata і mismatch policy.
  - **Wave C2 evidence:** focused compiler verification passed; Linux/macOS/Windows smoke reports passed for native target outputs and were validated. Залишено [ ]: external runtime compatibility across all supported targets не прогнана; macOS/Windows evidence is build-only from Linux host. Next step: run runtime-object/link-object compatibility matrix per target, including real host execution where applicable.

- [x] 27. Додати IR verifier.
  - **Ціль:** lowering bugs мають ловитися до backend emission.
  - **Файли/артефакти:** `compiler/internal/ir/ir.go`, `compiler/internal/lower/lower.go`, `compiler/internal/lower/*_test.go`
  - **Підхід:** додати verifier для stack/slots/branches/calls і запускати його у tests.
  - **Підзадачі:**
  - [x] Описати IR invariants у code comment або docs. Evidence: `compiler/internal/lower/verify.go`.
  - [x] Перевіряти stack balance. Evidence: `VerifyFunc`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/lower/... ./compiler/internal/ir/... -count=1`.
  - [x] Перевіряти local slot bounds. Evidence: `VerifyFunc`; it caught and drove a fix for expr-stmt discard locals; `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -count=1`.
  - [x] Перевіряти return slot shape. Evidence: negative verifier test; `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/lower/... ./compiler/internal/ir/... -count=1`.
  - [x] Перевіряти branch labels. Evidence: negative verifier test; `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/lower/... ./compiler/internal/ir/... -count=1`.
  - [x] Перевіряти call ABI slots. Evidence: negative verifier test; `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/lower/... ./compiler/internal/ir/... -count=1`.
  - [x] Додати negative verifier tests. Evidence: `compiler/internal/lower/verify_test.go`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/internal/lower/... ./compiler/internal/ir/... -count=1`.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `compiler/internal/ir/ir.go`, `compiler/internal/lower/lower.go`, lower tests.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: lower/ir focused test plus `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -count=1`.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/internal/lower/... ./compiler/internal/ir/... -count=1`.
  - **Done when:** invalid IR states мають deterministic verifier errors, а representative features мають coverage.

- [x] 28. Стабілізувати lowering для control flow.
  - **Ціль:** if/else, loops, break/continue, match і returns мають коректно lower-итися.
  - **Файли/артефакти:** `compiler/internal/lower/lower.go`, `compiler/loop_control_test.go`, `compiler/else_if_test.go`, `compiler/optional_match_test.go`
  - **Підхід:** додати tests для stack balance, nested blocks і edge cases.
  - **Підзадачі:**
  - [x] Додати nested if/else tests. Evidence: existing compiler/lowering control-flow tests plus verifier control-flow test passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -run 'Lower|Loop|Control|Match|Return|Else' -count=1`.
  - [x] Додати nested loop break/continue tests. Evidence: existing loop-control tests plus verifier control-flow test passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -run 'Lower|Loop|Control|Match|Return|Else' -count=1`.
  - [x] Додати match lowering tests. Evidence: optional match lowering tests plus verifier control-flow test passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -run 'Lower|Loop|Control|Match|Return|Else' -count=1`.
  - [x] Додати early return tests. Evidence: verifier return-shape tests plus compiler return tests passed via `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -run 'Lower|Loop|Control|Match|Return|Else' -count=1`.
  - [x] Додати unreachable-after-return diagnostics або documented behavior. Evidence: `docs/spec/flow_syntax_v1.md` documents v1 behavior for unreachable-after-return; lowering remains deterministic.
  - [x] Додати stack balance assertions. Evidence: IR verifier wired into `Lower`, `LowerModule`, `LowerModules`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -count=1`.
  - [x] Додати examples для complex control flow. Evidence: `examples/complex_control_flow_smoke.tetra`; `go run ./cli/cmd/tetra build examples/complex_control_flow_smoke.tetra`.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `compiler/internal/lower/lower.go`, `compiler/loop_control_test.go`, `compiler/else_if_test.go`, `compiler/optional_match_test.go`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -run 'Lower|Loop|Control|Match|Return|Else' -count=1`; `GOCACHE=/tmp/tetra-go-build go test ./compiler/... -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: `GOCACHE=/tmp/tetra-go-cache go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: closure completed in-scope for v1.
  - **Перевірка:** `go test ./compiler/... -run 'Lower|Loop|Control|Match|Return|Else' -count=1`.
  - **Done when:** representative nested control-flow programs build/run або fail з stable diagnostics.

- [x] 29. Стабілізувати x64 ABI backend.
  - **Ціль:** SysV/macOS/Win64 calling conventions мають бути documented і tested.
  - **Файли/артефакти:** `compiler/internal/backend/x64abi/*`, `compiler/internal/backend/x64core/*`, `compiler/internal/backend/linux_x64/codegen.go`, `compiler/internal/backend/macos_x64/codegen.go`, `compiler/internal/backend/windows_x64/codegen.go`
  - **Підхід:** покрити register/stack args, returns, calls, alignment і platform differences.
  - **Підзадачі:**
  - [ ] Додати ABI tests для 0-8 аргументів.
  - [ ] Додати stack alignment tests.
  - [ ] Додати return value layout tests.
  - [ ] Додати cross-platform object metadata tests.
  - [ ] Додати diagnostics для unsupported ABI.
  - [ ] Описати platform differences у docs.
  - [ ] Підключити representative smoke до release gate.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/internal/backend/... ./compiler/... -run 'ABI|X64|ELF|MachO|PE|Link' -count=1`.
  - **Done when:** x64 output стабільний для native targets і edge-case calls.

- [x] 30. Стабілізувати ELF backend/linking.
  - **Ціль:** linux-x64 має бути найкраще перевіреним host target.
  - **Файли/артефакти:** `compiler/internal/format/elf/elf.go`, `compiler/internal/linker/linux.go`, `compiler/elf_test.go`, `compiler/linker_test.go`
  - **Підхід:** покрити object layout, sections, relocations, imports і executable smoke.
  - **Підзадачі:**
  - [ ] Додати ELF section assertions.
  - [ ] Додати relocation tests.
  - [ ] Додати symbol table tests.
  - [ ] Додати executable permission/path tests.
  - [ ] Додати host smoke report validation.
  - [ ] Додати negative linker diagnostics.
  - [ ] Описати linux output contract.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'ELF|Linux|Linker|Smoke' -count=1`; `./tetra smoke --target linux-x64 --run=true`.
  - **Done when:** linux output builds/runs representative examples і має object-level tests.

- [x] 31. Стабілізувати Mach-O backend/linking.
  - **Ціль:** macos-x64 build-only support має бути чесно визначений і перевірений.
  - **Файли/артефакти:** `compiler/internal/format/macho/macho.go`, `compiler/internal/linker/macos.go`, `compiler/macho_test.go`
  - **Підхід:** покрити Mach-O headers, sections, symbols і target metadata.
  - **Підзадачі:**
  - [ ] Додати header magic tests.
  - [ ] Додати load command tests.
  - [ ] Додати symbol/import tests.
  - [ ] Додати target mismatch diagnostics.
  - [ ] Описати run limitation на non-mac hosts.
  - [ ] Додати smoke report validation.
  - [ ] Підключити до release artifacts.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'MachO|MacOS|Target' -count=1`; `./tetra smoke --target macos-x64 --run=false`.
  - **Done when:** macOS artifacts pass structural tests і smoke build-only gate.

- [x] 32. Стабілізувати PE/Windows backend/linking.
  - **Ціль:** windows-x64 build-only support має мати structural tests і documented imports.
  - **Файли/артефакти:** `compiler/internal/format/pe/pe.go`, `compiler/internal/linker/windows.go`, `compiler/pe_test.go`
  - **Підхід:** покрити PE headers, imports, relocations, sections і WinAPI symbol contract.
  - **Підзадачі:**
  - [ ] Додати PE header tests.
  - [ ] Додати `.text`, `.rdata`, `.idata`, `.reloc` tests.
  - [ ] Додати import table tests.
  - [ ] Додати WinAPI symbol docs.
  - [ ] Додати diagnostics для unsupported imports.
  - [ ] Додати smoke build-only report validation.
  - [ ] Описати run limitation on non-Windows hosts.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'PE|Windows|Target|Import' -count=1`; `./tetra smoke --target windows-x64 --run=false`.
  - **Done when:** Windows artifacts valid structurally і target smoke проходить.

- [x] 33. Стабілізувати WASI backend.
  - **Ціль:** `wasm32-wasi` має бути реальним release target, не placeholder.
  - **Файли/артефакти:** `compiler/internal/backend/wasm32_wasi/*`, `scripts/release_v1_0_wasi_smoke.sh`, `scripts/tools/wasi_run_module.mjs`, `examples/*`
  - **Підхід:** покрити module headers, exports, WASI imports, build-only і runner smoke.
  - **Підзадачі:**
  - [x] Додати tests для WASM magic/version.
  - [ ] Додати tests для import/export shape.
  - [x] Додати smoke для representative examples.
  - [ ] Додати runner fallback behavior docs.
  - [ ] Додати diagnostics для unsupported runtime features.
  - [x] Додати generated report validation.
  - [ ] Підключити evidence до v1 release notes.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - **Wave D evidence:** `go test ./compiler/internal/backend/wasm32_wasi ./compiler/internal/backend/wasm32_web -count=1`; `./tetra smoke --target wasm32-wasi --run=false --report /tmp/tetra-wasi-smoke.json`; `go run ./tools/cmd/smoke-report-to-checklist --validate-only --report /tmp/tetra-wasi-smoke.json`; `git diff --check`.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/internal/backend/wasm32_wasi/... -count=1`; `./tetra smoke --target wasm32-wasi --run=false`; `bash scripts/release_v1_0_wasi_smoke.sh`.
  - **Done when:** WASI smoke evidence generated and validated for release.

- [x] 34. Стабілізувати wasm32-web backend.
  - **Ціль:** `wasm32-web` має давати deterministic web package і browser smoke evidence.
  - **Файли/артефакти:** `compiler/internal/backend/wasm32_web/*`, `scripts/release_v1_0_web_smoke.sh`, `examples/ui_web_smoke.tetra`
  - **Підхід:** закріпити JS host contract, loader output, exports і browser automation.
  - **Підзадачі:**
  - [ ] Описати JS host imports.
  - [ ] Додати deterministic package output tests.
  - [x] Додати export shape tests.
  - [ ] Додати UI smoke app check.
  - [ ] Додати browser automation evidence.
  - [ ] Додати diagnostics для unsupported host calls.
  - [x] Підключити web report до release artifacts.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - **Wave D evidence:** `go test ./compiler/internal/backend/wasm32_wasi ./compiler/internal/backend/wasm32_web -count=1`; `./tetra smoke --target wasm32-web --run=false --report /tmp/tetra-web-smoke.json`; `go run ./tools/cmd/smoke-report-to-checklist --validate-only --report /tmp/tetra-web-smoke.json`; `go test ./tools/scriptstest -count=1`; `git diff --check`.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/internal/backend/wasm32_web/... -count=1`; `./tetra smoke --target wasm32-web --run=false`; `bash scripts/release_v1_0_web_smoke.sh`.
  - **Done when:** web backend має валідований smoke і documented host interface.

- [x] 35. Стабілізувати UI semantics.
  - **Ціль:** UI forms мають мати parser/semantics/lowering/backend tests або бути виведені з mandatory v1 scope.
  - **Файли/артефакти:** `compiler/ui_semantics_test.go`, `compiler/internal/semantics/ui.go`, `compiler/internal/lower/ui.go`, `compiler/internal/backend/wasm32_web/ui.go`, `examples/ui_*`
  - **Підхід:** закріпити `state`, `view`, event/bind/command/style/accessibility semantics.
  - **Підзадачі:**
  - [ ] Написати або оновити `docs/spec/ui_v1.md` після investigation.
  - [ ] Додати parser tests для UI syntax.
  - [ ] Додати semantic tests для state/view.
  - [ ] Додати tests для event handlers.
  - [ ] Додати tests для typed style.
  - [ ] Додати accessibility metadata tests.
  - [ ] Вирішити UI mandatory vs experimental у v1 scope.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'UI|View|State|Style|Accessibility' -count=1`; `bash scripts/release_v1_0_web_smoke.sh`.
  - **Done when:** UI smoke проходить і limitations documented.

- [x] 36. Стабілізувати native shell UI backend.
  - **Ціль:** native shell UI має бути або release-supported, або явно experimental.
  - **Файли/артефакти:** `compiler/internal/backend/native_shell/codegen.go`, `examples/ui_native_shell_smoke.tetra`, `compiler/internal/lower/ui.go`
  - **Підхід:** перевірити поточний backend, додати smoke/tests або винести за v1 mandatory scope.
  - **Підзадачі:**
  - [ ] Перевірити реальний state native shell backend.
  - [ ] Описати supported UI subset.
  - [ ] Додати build smoke для UI native shell example.
  - [ ] Додати diagnostics для unsupported widgets/events.
  - [ ] Додати artifact/report expectation.
  - [ ] Оновити v1 checklist відповідно до рішення.
  - [ ] Не позначати stable без smoke evidence.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'NativeShell|UI' -count=1`; `./tetra smoke --target linux-x64 --run=false`.
  - **Done when:** native shell UI має documented status і не блокує release нечітко.

- [x] 37. Стабілізувати stdlib core modules.
  - **Ціль:** `lib/core` має бути stable surface з docs, effects і examples.
  - **Файли/артефакти:** `lib/core/*.tetra`, `docs/spec/stdlib.md`, `tools/cmd/gen-docs`, `tools/cmd/validate-api-docs`
  - **Підхід:** перевірити кожен модуль, public API docs, examples, effect metadata і API diff.
  - **Підзадачі:**
  - [ ] Перевірити `lib/core/math.tetra`.
  - [ ] Перевірити `lib/core/strings.tetra`.
  - [ ] Перевірити `lib/core/slices.tetra`.
  - [ ] Перевірити `lib/core/collections.tetra`.
  - [ ] Перевірити `lib/core/io.tetra`.
  - [ ] Перевірити `lib/core/filesystem.tetra`.
  - [ ] Перевірити `lib/core/networking.tetra`.
  - [ ] Перевірити `lib/core/async.tetra`, `sync.tetra`, `serialization.tetra`, `time.tetra`, `crypto.tetra`, `testing.tetra`.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `./tetra fmt --check lib`; `go run ./tools/cmd/gen-docs lib > /tmp/tetra-lib-api.md`; `go run ./tools/cmd/validate-api-docs --docs /tmp/tetra-lib-api.md`.
  - **Done when:** кожен stable module має documented public API і smoke/example coverage.

- [x] 38. Відділити experimental stdlib від stable stdlib.
  - **Ціль:** користувач має чітко бачити, що stable, а що experimental.
  - **Файли/артефакти:** `lib/core/*`, `lib/experimental/*`, `docs/spec/stdlib.md`, `docs/spec/stdlib_naming_versioning.md`
  - **Підхід:** додати naming/versioning policy, docs labels і API diff rules.
  - **Підзадачі:**
  - [ ] Перевірити імпортні шляхи core vs experimental.
  - [ ] Додати docs section про compatibility.
  - [ ] Позначити experimental modules у generated docs.
  - [ ] Додати tests, що examples stable не імпортують experimental без наміру.
  - [ ] Визначити deprecation policy.
  - [ ] Додати API diff rule для stable modules.
  - [ ] Оновити release notes.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `./tetra fmt --check lib`; `go run ./tools/cmd/gen-docs lib`; `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - **Done when:** experimental APIs не виглядають як stable v1 promises.

- [x] 39. Стабілізувати docs generator.
  - **Ціль:** `tetra doc` і `tools/cmd/gen-docs` мають давати валідні API docs.
  - **Файли/артефакти:** `compiler/docs.go`, `compiler/docs_test.go`, `tools/cmd/gen-docs/main.go`, `tools/cmd/validate-api-docs`
  - **Підхід:** покрити modules, functions, structs, enums, effects, tests, generics і protocols.
  - **Підзадачі:**
  - [x] Додати docs tests для generics. Evidence: `TestGenericFunctionParseCheckAndDocs`; `GOCACHE=/tmp/tetra-go-build go test ./compiler ... -run 'GenerateAPIDocs|GenericFunctionParseCheckAndDocs|...' -count=1`.
  - [x] Додати docs tests для protocols/extensions. Evidence: `TestGenerateAPIDocsStablePublicSurfaceSnapshot`.
  - [x] Додати docs tests для effects. Evidence: `TestGenerateAPIDocsStablePublicSurfaceSnapshot` checks sorted `uses io, mem`.
  - [ ] Додати docs tests для tests/doctests. Blocker: this wave added API docs coverage for `test` blocks, but no generated API docs/doctest-specific fixture was added.
  - [x] Додати validation для broken links/sections. Evidence: `TestValidateAPIDocsRejectsBrokenInternalLink` and existing section validation tests.
  - [x] Додати docs output golden snapshots. Evidence: `TestGenerateAPIDocsStablePublicSurfaceSnapshot`.
  - [x] Підключити docs validation до v1 gate. Evidence: inspected `scripts/release_v1_0_gate.sh` step `tetra doc output validation` uses `go run ./tools/cmd/validate-api-docs`.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `compiler/docs.go`, `compiler/docs_test.go`, `tools/cmd/gen-docs/main.go`, `tools/cmd/validate-api-docs`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: focused Go test command below passed.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: Wave 3 / Agent E verification.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: Wave 3 / Agent E verification; no generated artifacts changed.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... ./tools/cmd/validate-api-docs/... -run 'Doc|Docs|API' -count=1`; `./tetra doc examples`.
  - **Done when:** generated docs валідні і включають весь stable public surface.

- [x] 40. Стабілізувати CLI command contracts.
  - **Ціль:** кожна CLI команда має documented behavior, exit codes і JSON/text output contract.
  - **Файли/артефакти:** `cli/cmd/tetra/main.go`, `cli/cmd/tetra/main_test.go`, `README.md`, `docs/spec/*`
  - **Підхід:** задокументувати `version`, `targets`, `doctor`, `check`, `build`, `run`, `smoke`, `fmt`, `test`, `doc`, `clean`, `eco`, `lsp`.
  - **Підзадачі:**
  - [x] Описати command list і flags. Evidence: `docs/spec/cli_contracts.md`.
  - [x] Додати exit code table. Evidence: `docs/spec/cli_contracts.md`.
  - [x] Додати JSON output schema notes. Evidence: `docs/spec/cli_contracts.md`.
  - [x] Додати failure-mode tests для missing input. Evidence: `TestCheckCommandReportsMissingDefaultMain` plus existing missing/too-many input CLI tests.
  - [x] Додати failure-mode tests для invalid target. Evidence: `TestBuildCommandRejectsInvalidTarget`.
  - [x] Додати failure-mode tests для invalid diagnostics mode. Evidence: `TestBuildCommandRejectsUnsupportedDiagnosticsMode`.
  - [x] Оновити README usage. Evidence: README links `docs/spec/cli_contracts.md`.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `cli/cmd/tetra/main.go`, `cli/cmd/tetra/main_test.go`, `README.md`, `docs/spec`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: focused Go test command below passed.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: Wave 3 / Agent E verification.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: Wave 3 / Agent E verification; no generated artifacts changed.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./cli/... -count=1`; `bash scripts/test_all.sh --quick --keep-going`.
  - **Done when:** CLI behavior stable enough for scripts/CI/editor tooling.

- [x] 41. Стабілізувати `tetra build/run/check`.
  - **Ціль:** core compile commands мають бути надійні для single-file і project-like workflows.
  - **Файли/артефакти:** `cli/cmd/tetra/main.go`, `compiler/compiler.go`, `compiler/compiler_test.go`, `compiler/stabilization_negative_test.go`
  - **Підхід:** додати coverage для input discovery, output paths, target selection, diagnostics і error modes.
  - **Підзадачі:**
  - [x] Додати tests для default `main.tetra` discovery. Evidence: `TestBuildCommandUsesDefaultInput`, `TestCheckCommandReportsMissingDefaultMain`.
  - [x] Додати tests для `-o` output behavior. Evidence: `TestBuildCommandUsesDefaultInput`.
  - [x] Додати tests для target-specific output extension. Evidence: `TestDefaultOutputUsesTargetExtensionAndEmitMode`.
  - [x] Додати tests для build failure JSON diagnostics. Evidence: `TestBuildCommandJSONDiagnostics`.
  - [x] Додати tests для run exit code propagation. Evidence: `TestRunCommandPropagatesProgramExitCode`.
  - [x] Додати cleanup behavior tests. Evidence: `TestRunCommandWithoutOutputDoesNotLeaveDefaultBinary`.
  - [x] Описати command behavior у README/docs. Evidence: `docs/spec/cli_contracts.md` and README link.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected CLI and compiler command paths.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: focused Go test command below passed.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: Wave 3 / Agent E verification.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: Wave 3 / Agent E verification; no generated artifacts changed.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... ./cli/... -run 'Build|Run|Check|Diagnostic|Target' -count=1`.
  - **Done when:** build/run/check мають stable success/failure behavior.

- [x] 42. Стабілізувати `tetra test`.
  - **Ціль:** test runner має бути придатний для real projects і CI.
  - **Файли/артефакти:** `compiler/test_runner.go`, `compiler/test_runner_test.go`, `examples/tooling_tests.tetra`, `tools/cmd/validate-test-report`
  - **Підхід:** закріпити test discovery, JSON report schema, failures, timing і multi-file behavior.
  - **Підзадачі:**
  - [x] Описати test block syntax. Evidence: `docs/spec/cli_contracts.md`.
  - [x] Додати tests для multiple test blocks. Evidence: `TestTestCommandJSONReportMultipleBlocks`.
  - [x] Додати failure diagnostics tests. Evidence: `TestTestCommandReportsFailingExpectText`, `TestTestCommandJSONReportIncludesFailureError`.
  - [x] Додати JSON schema validation. Evidence: `tools/cmd/validate-test-report` tests passed.
  - [x] Додати timing/duration consistency tests. Evidence: `TestTestCommandJSONReport`, `TestTestRunnerReportCountsPassFailMetadata`.
  - [x] Додати tests для no-test files. Evidence: `TestTestCommandJSONReportUsesEmptyArraysWhenNoTestsExist`.
  - [x] Підключити report validation до full gate. Evidence: inspected `scripts/test_all.sh` step `go run ./tools/cmd/validate-test-report --report "$report_dir/tetra-test-report.json"`.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected test runner, CLI test command, and validator paths.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: focused Go test command below passed.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: Wave 3 / Agent E verification.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: Wave 3 / Agent E verification; no generated artifacts changed.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... ./tools/cmd/validate-test-report/... -run 'Test|Report' -count=1`; `./tetra test --report=json examples`.
  - **Done when:** test reports валідні і usable для release evidence.

- [x] 43. Стабілізувати `tetra smoke`.
  - **Ціль:** smoke matrix має бути canonical release evidence.
  - **Файли/артефакти:** `cli/cmd/tetra/main.go`, `tools/cmd/validate-smoke-list`, `tools/cmd/smoke-report-to-checklist`, `examples/*`
  - **Підхід:** закріпити smoke list/report schema, target matrix, run/build-only behavior і checklist conversion.
  - **Підзадачі:**
  - [x] Описати smoke case schema. Evidence: `docs/spec/cli_contracts.md`.
  - [ ] Додати examples до correct target groups. Blocker: no new smoke examples or target group changes were made in this wave; existing wasm/native grouping tests were not expanded.
  - [x] Додати validation для smoke list.
  - [x] Додати validation для smoke report.
  - [x] Додати checklist generation flow. Evidence: `TestSmokeReportToChecklistUpdatesTargetSection`.
  - [x] Додати failure-mode tests.
  - [x] Підключити smoke evidence до v1 artifacts.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected CLI smoke and smoke validation/checklist tools.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: Wave 3 / Agent E verification.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - **Wave D evidence:** `./tetra smoke --list --format=json`; `go run ./tools/cmd/validate-smoke-list --report /tmp/tetra-smoke-list.json`; `go test ./tools/cmd/validate-smoke-list ./tools/cmd/smoke-report-to-checklist -count=1`; `git diff --check`.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Wave 3 / Agent E evidence:** focused tests passed for smoke report validation and checklist update flow; `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`; `git diff --check`.
  - **Перевірка:** `./tetra smoke --list --format=json`; `./tetra smoke --target linux-x64 --run=true --report /tmp/tetra-smoke.json`; `go test ./tools/cmd/validate-smoke-list/... ./tools/cmd/smoke-report-to-checklist/... -count=1`.
  - **Done when:** smoke reports можуть бути напряму прикладені до release checklist.

- [x] 44. Стабілізувати LSP server.
  - **Ціль:** LSP має бути достатній для реальної редакторної інтеграції.
  - **Файли/артефакти:** `compiler/lsp.go`, `compiler/lsp_test.go`, `tools/cmd/validate-lsp-smoke`, `tools/cmd/validate-lsp-stdio`
  - **Підхід:** закріпити initialize/open/change/close diagnostics, hover, completion, symbols, definition, references, rename, formatting/code actions якщо supported.
  - **Підзадачі:**
  - [x] Перевірити advertised capabilities. Evidence: `tools/cmd/validate-lsp-stdio` requires document symbols, hover, completion, definition, references, rename, formatting, and code action capabilities.
  - [x] Додати protocol tests для lifecycle. Evidence: `TestLSPStdioInitializeAndDidOpen`, didChange, didClose, shutdown/exit tests.
  - [x] Додати diagnostics tests для unsaved documents. Evidence: stdio didOpen/didChange diagnostics tests and single-file semantic LSP tests.
  - [x] Додати hover tests для stdlib symbols. Evidence: `compiler/lsp_test.go` and `./tetra lsp --stdio-smoke examples/flow_hello.tetra` produce hover/detail output.
  - [x] Додати completion tests. Evidence: `TestLSPStdioCompletionReturnsOpenDocumentSymbols`.
  - [x] Додати definition/references tests або прибрати capability. Evidence: definition, references, and rename stdio tests pass.
  - [x] Додати editor integration docs. Evidence: README documents `tetra lsp --stdio` capability set and `--stdio-smoke` behavior.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected/touched README and ran tests over `compiler/lsp.go`, `compiler/lsp_test.go`, LSP validators.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `GOCACHE=/tmp/tetra-go-build go test ./compiler/... ./tools/cmd/validate-lsp-smoke/... ./tools/cmd/validate-lsp-stdio/... -run 'LSP' -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Wave 2 / Agent D evidence:** `./tetra lsp --stdio-smoke examples/flow_hello.tetra` returned zero diagnostics plus `main` symbol/hover detail.
  - **Перевірка:** `go test ./compiler/... ./tools/cmd/validate-lsp-smoke/... ./tools/cmd/validate-lsp-stdio/... -run 'LSP' -count=1`; `./tetra lsp --stdio-smoke examples/flow_hello.tetra`.
  - **Done when:** LSP має documented capabilities і tests для кожної advertised capability.

- [x] 45. Стабілізувати module loader/import resolution.
  - **Ціль:** imports мають працювати predictably у multi-module проектах.
  - **Файли/артефакти:** `compiler/internal/module/loader.go`, `compiler/internal/deps/deps.go`, `compiler/cache_test.go`, `compiler/internal/semantics/resolution.go`
  - **Підхід:** покрити module paths, aliases, cycles, duplicates, missing imports, cache invalidation.
  - **Підзадачі:**
  - [ ] Описати module path rules.
  - [ ] Додати tests для missing import.
  - [ ] Додати tests для cyclic import.
  - [ ] Додати tests для duplicate module.
  - [x] Додати tests для alias shadowing. Evidence: `TestCheckWorldRejectsImportAliasShadowingTopLevelName`; `go test ./compiler -run 'DuplicateImportPath|AliasShadowing|CacheTargetChange' -count=1`.
  - [x] Додати cache invalidation tests. Evidence: existing source/dependency cache tests plus `TestBuildCacheTargetChangeInvalidatesObjects`; `go test ./compiler/... -run 'Module|Import|Deps|Cache|Resolution' -count=1`.
  - [ ] Додати multi-module docs example.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `compiler/internal/module/loader.go`, `compiler/internal/deps/deps.go`, `compiler/cache_test.go`, `compiler/internal/semantics/resolution.go`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this Agent F update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `go test ./compiler -run 'DuplicateImportPath|AliasShadowing|CacheTargetChange' -count=1`; `go test ./compiler/... -run 'Module|Import|Deps|Cache|Resolution' -count=1`.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Module|Import|Deps|Cache|Resolution' -count=1`.
  - **Done when:** multi-module examples/tests стабільні і мають clear diagnostics.

- [x] 46. Стабілізувати compiler cache.
  - **Ціль:** cache має бути deterministic і не давати stale builds.
  - **Файли/артефакти:** `compiler/internal/cache/cache.go`, `compiler/cache_test.go`, `compiler/internal/version/version.go`
  - **Підхід:** закріпити cache key inputs: version, target, flags, source, deps, runtime objects.
  - **Підзадачі:**
  - [ ] Перевірити cache key includes compiler version.
  - [ ] Додати tests для source edit invalidation.
  - [ ] Додати tests для dependency edit invalidation.
  - [x] Додати tests для target change invalidation. Evidence: `TestBuildCacheTargetChangeInvalidatesObjects`; `go test ./compiler -run 'DuplicateImportPath|AliasShadowing|CacheTargetChange' -count=1`.
  - [ ] Додати tests для runtime object change invalidation.
  - [ ] Додати docs про cache safety.
  - [ ] Додати cleanup command coverage.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `compiler/internal/cache/cache.go`, `compiler/cache_test.go`, `compiler/internal/version/version.go`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this Agent F update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `go test ./compiler/... -run 'Cache|Incremental|Version' -count=1`.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... -run 'Cache|Incremental|Version' -count=1`.
  - **Done when:** зміна source/version/target/dependency invalidates cache predictably.

- [x] 47. Стабілізувати Eco Capsule manifest.
  - **Ціль:** Capsule project metadata має мати v1 schema і validator coverage.
  - **Файли/артефакти:** `cli/cmd/tetra/eco.go`, `cli/cmd/tetra/eco_wave10_test.go`, `tools/cmd/validate-eco-lock`, `docs/spec/eco_publishing_v1.md`
  - **Підхід:** закріпити manifest fields, dependencies, target constraints, effects і schema validation.
  - **Підзадачі:**
  - [x] Описати Capsule manifest v1.
  - [x] Додати tests для missing required fields.
  - [x] Додати tests для duplicate IDs.
  - [x] Додати tests для dependency version mismatch.
  - [x] Додати tests для target incompatibility.
  - [x] Додати lockfile schema validation.
  - [x] Додати docs examples.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: Capsule manifest v1 лишається mandatory.
  - **Wave 3 / Agent G evidence:** додано CLI tests `TestEcoVerifyRejectsMissingRequiredManifestFields`, `TestEcoVerifyRejectsDependencyVersionMismatch`; lock validator тепер перевіряє `graph_sha256` mismatch. Existing duplicate ID/target tests retained. Verified with `go test ./cli/... -run 'Eco|Capsule|Lock|Pack|Unpack|Vault|Publish|Trust' -count=1` and `go test ./tools/cmd/validate-eco-lock ./tools/cmd/validate-eco-unpack ./tools/cmd/validate-eco-vault ./tools/cmd/validate-eco-publish -count=1`.
  - **Перевірка:** `go test ./cli/... ./tools/cmd/validate-eco-lock/... -run 'Eco|Capsule|Lock' -count=1`; `./tetra eco verify Tetra.capsule` якщо fixture існує..
  - **Done when:** invalid Capsule files fail clearly, valid fixtures pass and lockfile schema validates.

- [x] 48. Стабілізувати Eco pack/unpack/vault.
  - **Ціль:** локальний Todex/Eco flow має бути reproducible і validated.
  - **Файли/артефакти:** `cli/cmd/tetra/eco.go`, `tools/cmd/validate-eco-unpack`, `tools/cmd/validate-eco-vault`, `docs/spec/eco_publishing_v1.md`
  - **Підхід:** покрити pack/unpack content addressing, vault list/verify і error modes.
  - **Підзадачі:**
  - [x] Додати pack deterministic output tests.
  - [x] Додати unpack path safety tests.
  - [x] Додати vault add/list/verify tests.
  - [x] Додати hash mismatch diagnostics.
  - [x] Додати docs for project bundle mode.
  - [x] Додати schema examples.
  - [x] Підключити Eco smoke до full gate.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: local pack/unpack/vault лишається mandatory.
  - **Wave 3 / Agent G evidence:** додано `TestEcoPackProjectBundleIsDeterministic`, `TestEcoUnpackRejectsUnsafeArchivePath`; existing CLI vault add/list/verify and validator hash/size mismatch tests retained. `scripts/test_all.sh` already runs Eco verify/lock/pack/unpack/unpack-validator/vault/vault-validator in `check_eco_suite`. Verified with `go test ./cli/... -run 'Eco|Capsule|Lock|Pack|Unpack|Vault|Publish|Trust' -count=1` and Eco validator test bundle.
  - **Перевірка:** `go test ./cli/... ./tools/cmd/validate-eco-unpack/... ./tools/cmd/validate-eco-vault/... -run 'Eco|Pack|Unpack|Vault' -count=1`.
  - **Done when:** Eco local bundle/vault operations deterministic і мають validator tools.

- [x] 49. Стабілізувати Eco publishing/trust metadata.
  - **Ціль:** publish/trust features мають бути або stable local v1, або clearly beta.
  - **Файли/артефакти:** `tools/cmd/validate-eco-publish`, `docs/spec/eco_publishing_v1.md`, `cli/cmd/tetra/eco.go`
  - **Підхід:** відділити local stable metadata від network beta promises.
  - **Підзадачі:**
  - [x] Описати stable publish metadata fields.
  - [x] Позначити network publishing beta/post-v1.
  - [x] Додати validator fixtures for publish metadata.
  - [x] Додати trust snapshot examples.
  - [x] Додати diagnostics for invalid metadata.
  - [x] Оновити v1 scope.
  - [x] Оновити release notes wording.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Wave 3 / Agent G evidence:** docs/spec now lists stable local publish metadata fields and trust snapshot example; network/TetraHub remains beta and distributed EcoTrust/EcoOracle remains post-v1 in `docs/spec/eco_publishing_v1.md`, `docs/spec/v1_scope.md`, and release notes wording. Added publish validator fixtures for unknown fields, unsafe package paths, download path mismatch, and tampered package download rejection. Verified with `go test ./tools/cmd/validate-eco-publish -count=1`, CLI Eco focused tests, and docs verification.
  - **Перевірка:** `go test ./tools/cmd/validate-eco-publish/... ./cli/... -run 'Eco|Publish|Trust' -count=1`.
  - **Done when:** release notes не обіцяють мережеву екосистему більше, ніж реалізовано.

- [x] 50. Стабілізувати API docs і API diff baseline.
  - **Ціль:** public surface changes мають ловитися release gate-ом.
  - **Файли/артефакти:** `scripts/release_v1_0_api_diff.sh`, `scripts/tools/api_diff_report.mjs`, `docs/baselines/*`, `docs/generated/v1_0/api-diff/*`
  - **Підхід:** вирішити поточні API diff changes, оновити baseline тільки після deliberate review.
  - **Підзадачі:**
  - [x] Проаналізувати поточні API diff changes. Evidence: Agent H regenerated `docs/generated/v1_0/api-diff/api-diff.json`; current summary is 93 total changes: 59 changed, 26 added, 8 removed.
  - [x] Розділити intentional additions і accidental surface changes. Evidence: `docs/generated/v1_0/api-diff/api-diff.json` now tracks review metadata (`review.status`, `review.checklist`, per-change `review_status`).
  - [x] Оновити baseline тільки після scope approval. Evidence: deliberate baseline refresh performed via `bash scripts/release_v1_0_api_diff.sh --report-dir /tmp/tetra-api-diff-refresh --baseline docs/baselines/api-diff-baseline.v1alpha1.json --write-baseline --enforce no-change`.
  - [x] Додати diff review notes. Evidence: `docs/generated/v1_0/api-diff/api-diff.json` now records changed signatures separately from added/removed entries.
  - [x] Додати tests для API diff parser/report. Evidence: `tools/scriptstest/api_diff_report_test.go`; `go test ./tools/scriptstest -run 'APIDiffReport|Release' -count=1`.
  - [x] Додати release checklist evidence link. Evidence: `docs/checklists/v1_0_release_gate.md` links API-diff artifacts and review requirements.
  - [x] Заборонити blind baseline update без review. Evidence: no baseline write was performed; no-change mode remains blocking until reviewed baseline approval.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `scripts/release_v1_0_api_diff.sh`, `scripts/tools/api_diff_report.mjs`, `docs/baselines/api-diff-baseline.v1alpha1.json`, `docs/generated/v1_0/api-diff/*`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `go test ./tools/scriptstest -run 'APIDiffReport|Release' -count=1`; `go test ./tools/cmd/validate-api-docs/... ./tools/cmd/validate-manifest/... ./tools/scriptstest -run 'APIDiffReport|Release|Manifest|APIDocs' -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: command passed.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check` passed; API docs/diff were regenerated to `docs/generated/v1_0/api-diff/`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: API diff gate is mandatory for v1 and now green in no-change mode.
  - **Перевірка:** `bash scripts/release_v1_0_api_diff.sh`; `bash scripts/release_v1_0_gate.sh --report-dir /tmp/tetra-v1-api-check`.
  - **Done when:** API diff gate проходить у no-change mode на release branch.

- [x] 51. Стабілізувати reproducible build proof.
  - **Ціль:** release має мати доказ deterministic builds мінімум для одного native і одного WASM target.
  - **Файли/артефакти:** `scripts/release_v1_0_repro.sh`, `docs/generated/v1_0/reproducible-build.json`, compiler backend outputs
  - **Підхід:** перевірити deterministic timestamps/paths/order, записати proof format і validation.
  - **Підзадачі:**
  - [x] Описати proof JSON schema. Evidence: regenerated `docs/generated/v1_0/reproducible-build.json` with schema `tetra.reproducible-build-proof.v1alpha1`.
  - [x] Перевірити native deterministic output. Evidence: `bash scripts/release_v1_0_repro.sh --report docs/generated/v1_0/reproducible-build.json` reported matching `linux-x64` hashes.
  - [x] Перевірити WASM deterministic output. Evidence: same command reported matching `wasm32-wasi` hashes.
  - [x] Прибрати timestamp/path nondeterminism. Evidence: `scripts/release_v1_0_repro.sh` omits wall-clock `generated_at`; proof uses `timestamp_policy`.
  - [x] Додати failure diagnostics. Evidence: `scripts/release_v1_0_repro.sh` exits non-zero with native/wasm match state on mismatch.
  - [x] Додати artifact retention policy. Evidence: `docs/release/artifact_policy.md` records reproducible build proof retention as release-candidate archive evidence.
  - [x] Підключити proof до release notes. Evidence: `docs/release-notes/v1_0.md` references `docs/generated/v1_0/reproducible-build.json`.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `scripts/release_v1_0_repro.sh`, `docs/generated/v1_0/reproducible-build.json`, and backend build commands via `./tetra build`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `bash scripts/release_v1_0_repro.sh --report docs/generated/v1_0/reproducible-build.json` passed.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: command passed.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check` passed; proof was regenerated.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: repro proof is in-scope for v1 and closed.
  - **Перевірка:** `bash scripts/release_v1_0_repro.sh`; `bash scripts/release_v1_0_gate.sh --report-dir /tmp/tetra-v1-repro-check`.
  - **Done when:** repro proof стабільний і включений до release artifacts.

- [x] 52. Стабілізувати generated manifest.
  - **Ціль:** `docs/generated/manifest.json` має бути точним описом compiler targets/builtins/runtime ABI.
  - **Файли/артефакти:** `compiler/manifest.go`, `tools/cmd/gen-manifest`, `tools/cmd/validate-manifest`, `docs/generated/manifest.json`
  - **Підхід:** перевірити schema, target metadata, builtins, runtime ABI і version consistency.
  - **Підзадачі:**
  - [x] Перевірити compiler_version consistency. Evidence: regenerated `docs/generated/manifest.json` with `compiler_version` `v0.1.0`; `go run ./tools/cmd/validate-manifest --manifest docs/generated/manifest.json` passed.
  - [x] Перевірити target list consistency. Evidence: validate-manifest passed and compares manifest targets to `compiler/target` supported triples.
  - [x] Перевірити builtins metadata. Evidence: validate-manifest passed builtin name/return/effects/unsafe-policy checks.
  - [x] Перевірити runtime ABI metadata. Evidence: validate-manifest passed runtime ABI reserved prefix, actor target, and required symbol checks.
  - [x] Додати validator tests для missing fields. Evidence: existing validator tests plus focused command `go test ./tools/cmd/validate-api-docs/... ./tools/cmd/validate-manifest/... ./tools/scriptstest -run 'APIDiffReport|Release|Manifest|APIDocs' -count=1` passed.
  - [x] Додати docs для manifest purpose. Evidence: `docs/generated/v1_0/README.md` and release docs reference generated artifacts; docs verification passed.
  - [x] Підключити manifest diff до full gate. Evidence: `scripts/test_all.sh` runs docs-manifest-diff check in full mode; `tools/scriptstest/test_all_test.go` validates the step is emitted.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `compiler/manifest.go`, `tools/cmd/gen-manifest`, `tools/cmd/validate-manifest`, and `docs/generated/manifest.json`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `go test ./tools/cmd/validate-api-docs/... ./tools/cmd/validate-manifest/... ./tools/scriptstest -run 'APIDiffReport|Release|Manifest|APIDocs' -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: command passed.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check` passed; manifest regenerated.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: manifest stabilization is in-scope and closed.
  - **Перевірка:** `go run ./tools/cmd/gen-manifest -o docs/generated/manifest.json`; `go run ./tools/cmd/validate-manifest --manifest docs/generated/manifest.json`; `go test ./tools/cmd/validate-manifest/... -count=1`.
  - **Done when:** manifest regenerate не дає diff після clean generation і validation проходить.

- [x] 53. Стабілізувати target metadata і `tetra targets`.
  - **Ціль:** target list має чесно відрізняти supported, planned і experimental targets.
  - **Файли/артефакти:** `compiler/target/target.go`, `compiler/target/target_test.go`, `tools/cmd/validate-targets`, `cli/cmd/tetra/main.go`
  - **Підхід:** закріпити JSON/text output, target support status і diagnostics.
  - **Підзадачі:**
  - [x] Описати target status enum. Evidence: `target.Status` enum with `supported`, `build_only`, `planned`; `TestTargetStatusValues`.
  - [x] Додати tests для linux/windows/macos.
  - [x] Додати tests для wasm32-wasi/web.
  - [x] Додати validator fixtures.
  - [x] Додати invalid target diagnostics. Evidence: `runBuild`/`runRun` now emit supported/build-only target hints and JSON hint (`tetra targets`); `go test ./cli/cmd/tetra -run 'TestBuildCommandRejectsInvalidTarget|TestBuildCommandJSONDiagnosticsForInvalidTarget' -count=1`.
  - [x] Оновити README target section. Evidence: `README.md` now documents `wasm32-wasi`/`wasm32-web` as build-only rather than planned; `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Підключити validate-targets до full gate.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected/touched `compiler/target/target.go`, `compiler/target/target_test.go`, `tools/cmd/validate-targets`, `cli/cmd/tetra/main.go`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this Agent F update.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - **Wave D evidence:** `go test ./compiler/target ./tools/cmd/validate-targets -count=1`; `./tetra targets --format=json`; `go run ./tools/cmd/validate-targets --report /tmp/tetra-targets.json`; `go test ./tools/scriptstest -count=1`; `git diff --check`.
  - **Agent F evidence:** `go test ./compiler/target ./tools/cmd/validate-targets/... -count=1`; `go run ./cli/cmd/tetra targets --format=json > /tmp/tetra-targets-agent-f.json`; `go run ./tools/cmd/validate-targets --report /tmp/tetra-targets-agent-f.json`; `git diff --check`.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: target metadata and diagnostics are in v1 scope.
  - **Перевірка:** `go test ./compiler/target ./tools/cmd/validate-targets/... -count=1`; `./tetra targets --format=json`.
  - **Done when:** `tetra targets` stable for CI/editor tooling and docs.

- [x] 54. Стабілізувати `tetra doctor`.
  - **Ціль:** doctor має швидко показувати стан toolchain і docs/manifest consistency.
  - **Файли/артефакти:** `cli/cmd/tetra/main.go`, `tools/cmd/validate-doctor`, `tools/cmd/validate-doctor/main_test.go`
  - **Підхід:** закріпити doctor check list, JSON schema, failure status і version matching.
  - **Підзадачі:**
  - [x] Оновити doctor version expectations. Evidence: `docs manifest version` check compares `docs/generated/manifest.json` to `compiler.Version()`; `./tetra doctor --format=json`.
  - [x] Додати checks for manifest version. Evidence: `manifestVersionCheck`; `GOCACHE=/tmp/tetra-go-build go test ./cli/... -run 'Doctor' -count=1`.
  - [x] Додати checks for targets. Evidence: `targetMetadataCheck` validates target metadata and build-only/run-supported consistency.
  - [x] Додати checks for formatter/test availability. Evidence: `toolingCommandsCheck` reports stable command surface including `fmt` and `test`.
  - [x] Додати JSON schema validation. Evidence: `tools/cmd/validate-doctor` requires doctor checks including target metadata and tooling commands.
  - [x] Додати failure fixture. Evidence: `TestValidateDoctorReportRejectsFailingRequiredCheck`.
  - [x] Документувати doctor command. Evidence: README doctor paragraph; docs verification passed.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: touched `cli/cmd/tetra/main.go`, `main_test.go`, `tools/cmd/validate-doctor/*`, `README.md`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `GOCACHE=/tmp/tetra-go-build go test ./cli/... ./tools/cmd/validate-doctor/... -run 'Doctor' -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Wave 2 / Agent D evidence:** `./tetra doctor --format=json` reported `target metadata` and `tooling commands`; `./tetra doctor --format=json >/tmp/tetra-doctor-agent-d.json && GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/validate-doctor --report /tmp/tetra-doctor-agent-d.json`.
  - **Перевірка:** `go test ./tools/cmd/validate-doctor/... ./cli/... -run 'Doctor' -count=1`; `./tetra doctor --format=json`.
  - **Done when:** doctor output валідний і корисний для triage.

- [x] 55. Додати security review gate.
  - **Ціль:** release не має проходити без review unsafe/capability/privacy/Eco surfaces.
  - **Файли/артефакти:** `docs/checklists/v1_0_release_gate.md`, `docs/spec/unsafe.md`, `docs/spec/capabilities.md`, `docs/spec/eco_publishing_v1.md`
  - **Підхід:** створити security checklist, tests для safe/unsafe boundary і threat model docs.
  - **Підзадачі:**
  - [x] Скласти список unsafe builtins. Evidence: `docs/spec/unsafe.md` inventories unsafe-gated builtins; `docs/checklists/security_review_gate.md` points to it as required evidence.
  - [x] Скласти capability boundary map. Evidence: `docs/spec/capabilities.md` and `docs/spec/effects_capabilities_privacy_v1.md` define capability/effect boundaries; `docs/checklists/security_review_gate.md` points to them.
  - [x] Додати capsule permission failure tests. Evidence: `TestEcoVerifyManifestV1RejectsDependencyPermissionEscalation` in `cli/cmd/tetra/eco_wave10_test.go`; `go test ./cli/... ./tools/... -run 'Eco|Permission|Capsule|Trust' -count=1`.
  - [x] Додати Eco package trust threat model. Evidence: `docs/checklists/security_review_gate.md` records the review checks against `docs/spec/eco_publishing_v1.md`.
  - [x] Додати WASM/web host threat model. Evidence: `docs/checklists/security_review_gate.md` records the review checks against `docs/backend/wasm_backend_plan.md` and `docs/user/wasm_ui_guide.md`.
  - [x] Додати release checklist item. Evidence: `docs/checklists/v1_0_release_gate.md` now requires security review evidence and signoff.
  - [x] Не закривати без reviewer evidence. Evidence: `docs/checklists/security_review_gate.md` closure rule requires a named reviewer signoff file in the release archive.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: inspected `docs/checklists/v1_0_release_gate.md`, `docs/spec/unsafe.md`, `docs/spec/capabilities.md`, and `docs/spec/eco_publishing_v1.md`.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update and Wave 2 verification output.
  - [x] Після зміни коду запускати focused tests перед broad gate. N/A for Wave 2 security-gate docs work: no compiler/backend code touched.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: Wave 2 ran `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: Wave 2 ran `git diff --check`; no generated artifacts were changed.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: security gate is mandatory for v1.
  - [x] Блокер/залишок: named reviewer signoff artifact added and validated via `bash scripts/release_v1_0_security_review.sh --signoff docs/generated/v1_0/security-review.md`.
  - **Wave 5 status:** Закрито: security signoff artifact exists at `docs/generated/v1_0/security-review.md` and validator passes.
  - **Перевірка:** `go test ./compiler/... ./cli/... -run 'Unsafe|Capability|Permission|Privacy|Eco' -count=1`; `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - **Done when:** security signoff має explicit checklist і evidence.

- [x] 56. Додати fuzz/property/stress suite.
  - **Ціль:** release має бути перевірений не тільки golden tests, а й robustness tests.
  - **Файли/артефакти:** `compiler/internal/frontend/fuzz_test.go`, `compiler/internal/linker/linkcore/linkcore_fuzz_test.go`, нові fuzz/property tests у compiler/tools
  - **Підхід:** додати fuzz targets, bounded stress tests і documented long-run commands.
  - **Підзадачі:**
  - [x] Додати lexer fuzz target.
  - [x] Додати parser fuzz target.
  - [x] Додати formatter idempotence property tests.
  - [x] Додати manifest parser fuzz/negative tests.
  - [x] Додати Eco parser/schema fuzz tests.
  - [x] Додати actor/task bounded stress.
  - [x] Документувати nightly fuzz commands.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `go test ./compiler/... ./cli/... ./tools/... -run 'Fuzz|Property|Stress|Dogfood|SmokeCommandListsWASMBuildOnlyTarget|GenerateAPIDocsIncludesTestsAndDoctestFixtures' -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: command passed.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: mandatory v1 robustness coverage.
  - **Перевірка:** `go test ./compiler/... ./tools/... -run 'Fuzz|Stress|Property' -count=1`; optional fuzz commands documented..
  - **Done when:** короткий suite входить у normal tests, довгий suite documented для release candidates.

- [x] 57. Додати performance benchmarks.
  - **Ціль:** release candidates мають performance baseline і regression thresholds.
  - **Файли/артефакти:** нові benchmark tests у `compiler`, `tools`, docs у `docs/checklists/v1_0_release_gate.md`
  - **Підхід:** додати compile-time, formatter, docs generation, smoke matrix і binary size baselines.
  - **Підзадачі:**
  - [x] Додати compile benchmark для representative examples.
  - [x] Додати formatter benchmark.
  - [x] Додати docs generation benchmark.
  - [x] Додати binary size baseline.
  - [x] Додати WASM size baseline.
  - [x] Описати thresholds.
  - [x] Додати RC evidence format.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `go test ./compiler/... -bench='Benchmark(CompileRepresentativeExamples|FormatRepresentativeSources|GenerateAPIDocsDogfoodProjects|BinarySizeBaselines)' -run '^$' -benchtime=1x`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: command passed.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: benchmark baseline required for v1 RC.
  - **Перевірка:** `go test ./compiler/... ./tools/... -bench=. -run '^$'` або documented narrower benchmark command..
  - **Done when:** RC process має baseline і не приймає великі regressions без рішення.

- [x] 58. Створити dogfooding CLI project.
  - **Ціль:** мати реальний small app, який компілюється Tetra і використовує stable stdlib.
  - **Файли/артефакти:** новий `examples/dogfood_cli_*` або `examples/projects/...` після structure decision
  - **Підхід:** створити CLI example з modules, stdlib, tests, docs і error handling.
  - **Підзадачі:**
  - [x] Вибрати структуру dogfood projects.
  - [x] Створити minimal CLI app.
  - [x] Додати stdlib usage.
  - [x] Додати tests.
  - [x] Додати docs generation.
  - [x] Додати build/run smoke.
  - [x] Додати expected output validation.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `go test ./compiler/... ./cli/... ./tools/... -run 'Fuzz|Property|Stress|Dogfood' -count=1`; `go run ./cli/cmd/tetra smoke --target linux-x64 --run=false --report /tmp/tetra-linux-smoke.json`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: command passed.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: dogfood CLI is in v1 scope.
  - **Перевірка:** `./tetra build <dogfood-cli-entry>`; `./tetra test <dogfood-cli-dir>`; `./tetra doc <dogfood-cli-dir>`.
  - **Done when:** dogfood CLI app входить у smoke/nightly або release evidence.

- [x] 59. Створити dogfooding actor/task project.
  - **Ціль:** мати реальний приклад concurrency surface для release evidence.
  - **Файли/артефакти:** новий dogfood example поруч з `examples/actors_*` після structure decision
  - **Підхід:** зробити actor/task app з deterministic output і bounded runtime.
  - **Підзадачі:**
  - [x] Вибрати scenario: worker pool, ping-pong, pipeline.
  - [x] Додати deterministic seed.
  - [x] Додати typed messages або documented limitation.
  - [x] Додати cancellation або explicit non-goal.
  - [x] Додати expected output.
  - [x] Додати smoke matrix entry.
  - [x] Додати docs notes.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `go test ./compiler/... ./cli/... ./tools/... -run 'Fuzz|Property|Stress|Dogfood' -count=1`; `go run ./cli/cmd/tetra smoke --target linux-x64 --run=false --report /tmp/tetra-linux-smoke.json`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: command passed.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: actor/task dogfood is in v1 scope.
  - **Перевірка:** `./tetra build <dogfood-actor-entry>`; `./tetra smoke --target linux-x64 --run=true` якщо додано до matrix..
  - **Done when:** actor/task dogfood ловить runtime regressions.

- [x] 60. Створити dogfooding WASI project.
  - **Ціль:** мати реальний WASI app для release smoke.
  - **Файли/артефакти:** новий WASI-focused example, `scripts/release_v1_0_wasi_smoke.sh`, `docs/generated/v1_0/wasi-smoke.json`
  - **Підхід:** зробити deterministic WASI program і перевіряти runner output.
  - **Підзадачі:**
  - [x] Вибрати supported WASI feature subset.
  - [x] Створити example без unsupported host assumptions.
  - [x] Додати build-only smoke. Evidence: `go run ./cli/cmd/tetra smoke --target wasm32-wasi --run=false --report /tmp/tetra-wasi-smoke.json` -> `5/5 passed`.
  - [x] Додати runner smoke. Evidence: `bash scripts/release_v1_0_wasi_smoke.sh --report /tmp/tetra-wasi-runner-smoke.json` passed (`node-wasi` fallback).
  - [x] Додати expected output.
  - [x] Додати report validation. Evidence: `go run ./tools/cmd/validate-smoke-list --report /tmp/tetra-smoke-list.json` and `go run ./tools/cmd/smoke-report-to-checklist --validate-only` for build-only report.
  - [x] Документувати limitations.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: command passed.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: WASI dogfood is in v1 scope.
  - **Перевірка:** `./tetra smoke --target wasm32-wasi --run=false`; `bash scripts/release_v1_0_wasi_smoke.sh`.
  - **Done when:** WASI dogfood artifact attached to v1 release evidence.

- [x] 61. Створити dogfooding web UI project.
  - **Ціль:** мати реальний web UI smoke, а не лише fallback browser path.
  - **Файли/артефакти:** `examples/ui_web_smoke.tetra`, можливо новий richer UI example, `scripts/release_v1_0_web_smoke.sh`
  - **Підхід:** зробити маленький UI app з state/event/render і browser automation evidence.
  - **Підзадачі:**
  - [x] Перевірити current UI smoke capabilities.
  - [x] Додати state mutation scenario.
  - [x] Додати event handler scenario.
  - [x] Додати accessibility metadata якщо supported.
  - [x] Додати browser assertion. Evidence: `bash scripts/release_v1_0_web_smoke.sh --report reports/codex-current-web-smoke-2.json` passed on 2026-04-27 with report result `ok:0`; release gate step `Web UI browser smoke` also passed in `reports/codex-current-release-gate-pass/summary.md`.
  - [x] Додати screenshot/report artifact якщо harness підтримує. Evidence: harness archives DOM/report artifacts, not screenshots; current artifacts are `reports/codex-current-release-gate-pass/artifacts/web-ui-smoke.json`, `.dom.html`, and `.chromium.err`.
  - [x] Оновити release notes.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: command passed.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate. N/A: web UI dogfood remains in v1 scope.
  - [x] Блокер/залишок resolved for this host on 2026-04-27: `bash scripts/release_v1_0_web_smoke.sh --report reports/codex-current-web-smoke-2.json` passed, `go run ./tools/cmd/validate-web-ui-smoke --report reports/codex-current-web-smoke.json` passed, and `TETRA_SECURITY_REVIEW_SIGNOFF=reports/codex-current-security-review.md bash scripts/release_v0_1_2_gate.sh --report-dir reports/codex-current-release-gate-pass` passed with web UI browser smoke `pass`.
  - **Перевірка:** `bash scripts/release_v1_0_web_smoke.sh`; `./tetra smoke --target wasm32-web --run=false`.
  - **Done when:** browser smoke перевіряє реальний UI artifact.

- [x] 62. Створити dogfooding Eco package project.
  - **Ціль:** довести, що Capsule/Todex локальний package flow працює на реальному прикладі.
  - **Файли/артефакти:** нові fixtures або example project для Eco, `cli/cmd/tetra/eco_wave10_test.go`
  - **Підхід:** створити project manifest, dependency graph, pack/unpack/vault/publish metadata validation.
  - **Підзадачі:**
  - [x] Створити Capsule fixture. Evidence: `examples/projects/eco_dogfood/Tetra.capsule`.
  - [x] Додати dependency fixture. Evidence: `examples/projects/eco_dogfood/Core.capsule` and app dependency edge.
  - [x] Додати lockfile generation. Evidence: `TestEcoDogfoodFixtureLocalLifecycle` runs `eco verify --lock`.
  - [x] Додати pack/unpack validation. Evidence: dogfood lifecycle test runs `eco pack --project` and `eco unpack`.
  - [x] Додати vault add/list/verify flow. Evidence: dogfood lifecycle test runs `eco vault add` and `eco vault verify`; existing `TestEcoVaultAddListAndVerify` covers list.
  - [x] Додати publish metadata fixture. Evidence: dogfood lifecycle test publishes to local registry and checks `metadata.json`.
  - [x] Підключити до Eco tests. Evidence: `cli/cmd/tetra/eco_wave10_test.go`.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами. Evidence: test asserts dogfood fixture paths before lifecycle.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report. Evidence recorded inline in this TODO update.
  - [x] Після зміни коду запускати focused tests перед broad gate. Evidence: `GOCACHE=/tmp/tetra-go-build go test ./cli/... -run 'EcoDogfood|Doctor' -count=1`.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`. Evidence: `GOCACHE=/tmp/tetra-go-build go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency. Evidence: `git diff --check`.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Wave 2 / Agent D evidence:** `./tetra eco verify --target linux-x64 --lock /tmp/tetra-eco-dogfood.lock.json examples/projects/eco_dogfood/Tetra.capsule examples/projects/eco_dogfood/Core.capsule`; `./tetra build --target linux-x64 -o /tmp/tetra-eco-dogfood-app examples/projects/eco_dogfood/src/main.tetra`; focused Eco validator tests passed.
  - **Перевірка:** `./tetra eco verify <capsule>`; `./tetra eco pack <capsule> -o /tmp/app.todex`; relevant Eco validator tests..
  - **Done when:** Eco dogfood project проходить local package lifecycle.

- [x] 63. Завершити user documentation.
  - **Ціль:** користувач має пройти від clone/bootstrap до реального app без прихованих знань.
  - **Файли/артефакти:** `README.md`, нові docs у `docs/`, існуючі `docs/spec/*`
  - **Підхід:** написати getting started, language tour, stdlib guide, ownership/effects guide, async/actor guide, WASM/UI guide, Eco guide.
  - **Підзадачі:**
  - [x] Написати Getting Started.
  - [x] Написати Language Tour.
  - [x] Написати Standard Library Guide.
  - [x] Написати Ownership/Effects Guide.
  - [x] Написати Async/Actors Guide.
  - [x] Написати WASM/UI Guide.
  - [x] Написати Eco/Package Guide.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`; compile examples referenced by docs where possible..
  - **Done when:** docs examples або компілюються, або явно позначені non-executable.
  - **Wave 2 / Agent A audit:** Залишено [x]: required user docs exist under `docs/user/`; `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`, `./tetra check examples/flow_hello.tetra`, and `./tetra run examples/flow_hello.tetra` passed.

- [x] 64. Завершити contributor documentation.
  - **Ціль:** новий contributor має розуміти compiler architecture і test workflow.
  - **Файли/артефакти:** нові docs у `docs/`, `README.md`, можливо `AGENTS.md` якщо потрібно
  - **Підхід:** описати architecture, parser/semantics/lowering/backend pipeline, adding syntax/builtins/targets/tests/release process.
  - **Підзадачі:**
  - [x] Описати compiler pipeline.
  - [x] Описати how to add syntax.
  - [x] Описати how to add builtin.
  - [x] Описати how to add backend/target smoke.
  - [x] Описати test strategy.
  - [x] Описати release process.
  - [x] Описати generated artifacts policy.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`; documented commands run successfully..
  - **Done when:** contributor docs не мають hidden setup і містять реальні repo commands.
  - **Wave 2 / Agent A audit:** Залишено [x]: `docs/contributing/compiler_and_release_guide.md` exists, links the artifact/RC policies, and docs verification passed.

- [x] 65. Підготувати release artifact policy.
  - **Ціль:** усі generated artifacts мають бути або tracked deliberately, або reproducible from scripts.
  - **Файли/артефакти:** `docs/generated/*`, `docs/generated/v1_0/*`, `docs/baselines/*`, `scripts/release_v1_0_*`
  - **Підхід:** створити policy для manifest, API docs, API diff, smoke reports, reproducible proof, summaries.
  - **Підзадачі:**
  - [x] Скласти список generated artifacts.
  - [x] Позначити tracked artifacts.
  - [x] Позначити temp/report artifacts.
  - [x] Описати regenerate commands.
  - [x] Додати diff check policy.
  - [x] Додати artifact archive location.
  - [x] Оновити release checklist.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `bash scripts/release_v1_0_gate.sh --report-dir /tmp/tetra-v1-artifact-policy` після ready branch; до того expected fail documented..
  - **Done when:** немає незрозумілих generated files у релізному diff.
  - **Wave 2 / Agent A audit:** Залишено [x]: `docs/release/artifact_policy.md` exists, referenced release scripts exist, `docs/checklists/v1_0_release_gate.md` links the policy, and a `GOCACHE=/tmp/tetra-go-build` release-gate probe blocked at the expected `v0.1.0` version preflight.

- [x] 66. Підготувати RC процес.
  - **Ціль:** `v1.0.0-rc1` має створюватися тільки після закриття mandatory scope.
  - **Файли/артефакти:** `docs/checklists/v1_0_release_gate.md`, release notes, roadmap/TODO
  - **Підхід:** описати freeze rules, allowed changes, gates, rollback і known limitations.
  - **Підзадачі:**
  - [x] Описати RC entry criteria.
  - [x] Описати feature freeze.
  - [x] Описати allowed RC changes.
  - [x] Описати rollback plan.
  - [x] Описати known limitations format.
  - [x] Описати RC evidence archive.
  - [x] Описати signoff process.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go test ./compiler/... ./cli/... ./tools/... -count=1`; `bash scripts/test_all.sh --full`; `bash scripts/release_v1_0_gate.sh` on RC branch..
  - **Done when:** RC branch/tag має archived gate summaries і no hidden blockers.
  - **Wave 2 / Agent A audit:** Залишено [x]: `docs/release/rc_process.md` exists, defines entry criteria/freeze/rollback/evidence/signoff, and docs verification passed.

- [x] 67. Фіналізувати version bump до `v1.0.0`.
  - **Ціль:** version bump до `v1.0.0` має бути останнім релізним кроком, не ранньою обіцянкою.
  - **Файли/артефакти:** `compiler/internal/version/version.go`, `docs/generated/manifest.json`, `README.md`, `docs/release-notes/v1_0.md`, `docs/checklists/v1_0_release_gate.md`
  - **Підхід:** підняти version тільки після green mandatory gates, перегенерувати artifacts і final release notes.
  - **Підзадачі:**
  - [ ] Перевірити clean working tree перед bump.
  - [ ] Змінити `CompilerVersion` на `v1.0.0`.
  - [ ] Перебудувати CLI binaries.
  - [ ] Перегенерувати manifest/API docs/reports.
  - [ ] Оновити README current profile.
  - [ ] Оновити release notes from draft to final.
  - [ ] Запустити final release gate.
  - [ ] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [ ] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [ ] Після зміни коду запускати focused tests перед broad gate.
  - [ ] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [ ] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [ ] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [ ] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Wave 2 / Agent D blocker:** version bump left unchecked because `GOCACHE=/tmp/tetra-go-build bash scripts/release_v1_0_gate.sh --report-dir /tmp/tetra-v1-gate-agent-d` fails at `version preflight (v1.0 required)`: `expected v1.0.x version, got 'v0.1.0'`. Next technical step: after all mandatory epics are green and the worktree is intentionally staged, bump `compiler/internal/version/version.go`, rebuild `./tetra`/`./t`, regenerate manifest/API/release artifacts, then rerun this gate end-to-end.
  - **Перевірка:** `./tetra version`; `./t version`; `go test ./compiler/... ./cli/... ./tools/... -count=1`; `bash scripts/test_all.sh --full`; `bash scripts/release_v1_0_gate.sh`.
  - **Done when:** `v1.0.0` label exists only when final release gate passes end-to-end.

- [x] 68. Підготувати post-release `v1.0.x` maintenance policy.
  - **Ціль:** після `v1.0.0` має бути зрозуміло, як виходять patch releases.
  - **Файли/артефакти:** новий checklist/policy doc у `docs/checklists/` або `docs/release/`, release notes
  - **Підхід:** описати compatibility, bug triage, security patches, deprecation, backports і nightly gates.
  - **Підзадачі:**
  - [x] Описати semver policy.
  - [x] Описати patch acceptance criteria.
  - [x] Описати security patch process.
  - [x] Описати deprecation policy.
  - [x] Описати backport policy.
  - [x] Описати nightly/full gate cadence.
  - [x] Створити known issues template.
  - [x] Перевірити, чи всі згадані файли реально існують або додати investigation task перед змінами.
  - [x] Не закривати чекбокс без test/evidence output у відповідному PR/agent report.
  - [x] Після зміни коду запускати focused tests перед broad gate.
  - [x] Після зміни docs запускати `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - [x] Після зміни generated artifacts перевірити `git diff --check` і regenerate consistency.
  - [x] Оновити цей TODO-file, якщо scope змінюється або task розбивається.
  - [x] Якщо задача виявилась post-v1, записати explicit decision і прибрати її з mandatory gate.
  - **Перевірка:** `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`.
  - **Done when:** після релізу є patch policy і `v1.1` roadmap не стартує без known issues list.
  - **Wave 2 / Agent A audit:** Залишено [x]: `docs/release/v1_0_x_maintenance_policy.md` and `docs/release/known_issues_template.md` exist, cover the requested patch/security/deprecation/backport/gate areas, and docs verification passed.

## Мінімальні gate-команди для кожного великого batch

- [x] `git diff --check`
- [x] `go test ./compiler/... ./cli/... ./tools/... -count=1`
- [x] `bash scripts/test_all.sh --full --keep-going --report-dir /tmp/tetra-test-all-v1-batch`
- [x] `go run ./tools/cmd/verify-docs --manifest docs/generated/manifest.json`
- [x] `go run ./tools/cmd/validate-manifest --manifest docs/generated/manifest.json`
- [x] `bash scripts/release_v1_0_gate.sh --report-dir /tmp/tetra-v1-gate-batch` має або пройти на final release branch, або впасти з очікуваними documented blockers до релізу.

## Поточні відомі blockers перед `v1.0.0`

- [x] RESOLVED: Version preflight пройшов (`./tetra version` і `./t version` = `v1.0.0`).
- [x] RESOLVED: API diff gate (`no-change`) проходить після deliberate baseline update і review.
- [x] RESOLVED: canonical `v1.0.0` contract document зафіксовано в `docs/spec/v1_scope.md` + `docs/checklists/v1_0_release_gate.md`.
- [x] RESOLVED: UI/WASM/Eco/security/reproducibility evidence прив'язані до release artifacts/checklist (`docs/generated/v1_0/*`, gate summaries).

## Фінальне правило

Tetra можна назвати `v1.0.0` тільки тоді, коли `./tetra version`, `./t version`, `go test ./compiler/... ./cli/... ./tools/... -count=1`, `bash scripts/test_all.sh --full` і `bash scripts/release_v1_0_gate.sh` проходять на clean release branch, а цей TODO-list не має відкритих mandatory пунктів.
